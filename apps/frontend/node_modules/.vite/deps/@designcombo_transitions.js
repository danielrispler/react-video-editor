import {
  AbsoluteFill,
  Freeze,
  Internals,
  Sequence,
  interpolate,
  measureSpring,
  random,
  spring,
  useCurrentFrame,
  useVideoConfig
} from "./chunk-KZKZVUBZ.js";
import {
  require_react_dom
} from "./chunk-RJJ7WV7J.js";
import {
  require_jsx_runtime
} from "./chunk-LCQRLSVE.js";
import {
  require_react
} from "./chunk-23MI6R3T.js";
import {
  __toESM
} from "./chunk-4B2QHNJT.js";

// ../../node_modules/.pnpm/@designcombo+transitions@5.5.8_react-dom@19.2.5_react@19.2.5__react@19.2.5_remotion@4.0_144fab71c561b49c3f85e1b533dd12b5/node_modules/@designcombo/transitions/dist/index.es.js
var import_react2 = __toESM(require_react());

// ../../node_modules/.pnpm/@remotion+paths@4.0.340/node_modules/@remotion/paths/dist/esm/index.mjs
var cutLInstruction = ({
  instruction,
  lastPoint,
  progress
}) => {
  const x = lastPoint.x + (instruction.x - lastPoint.x) * progress;
  const y = lastPoint.y + (instruction.y - lastPoint.y) * progress;
  return {
    type: "L",
    x,
    y
  };
};
function interpolatePoint(pA, pB, factor) {
  return {
    x: pA.x + (pB.x - pA.x) * factor,
    y: pA.y + (pB.y - pA.y) * factor
  };
}
function cutCInstruction({
  progress,
  lastPoint,
  instruction
}) {
  const u = progress;
  const p0 = { x: lastPoint.x, y: lastPoint.y };
  const p1 = { x: instruction.cp1x, y: instruction.cp1y };
  const p2 = { x: instruction.cp2x, y: instruction.cp2y };
  const p3 = { x: instruction.x, y: instruction.y };
  const p01 = interpolatePoint(p0, p1, u);
  const p12 = interpolatePoint(p1, p2, u);
  const p23 = interpolatePoint(p2, p3, u);
  const p012 = interpolatePoint(p01, p12, u);
  const p123 = interpolatePoint(p12, p23, u);
  const p0123 = interpolatePoint(p012, p123, u);
  return {
    type: "C",
    cp1x: p01.x,
    cp1y: p01.y,
    cp2x: p012.x,
    cp2y: p012.y,
    x: p0123.x,
    y: p0123.y
  };
}
var cutInstruction = ({
  instruction,
  lastPoint,
  progress
}) => {
  if (instruction.type === "M") {
    return instruction;
  }
  if (instruction.type === "L") {
    return cutLInstruction({ instruction, lastPoint, progress });
  }
  if (instruction.type === "C") {
    return cutCInstruction({ instruction, lastPoint, progress });
  }
  if (instruction.type === "Z") {
    return instruction;
  }
  throw new TypeError(`${instruction.type} is not supported.`);
};
var tValues = [
  [],
  [],
  [
    -0.5773502691896257,
    0.5773502691896257
  ],
  [
    0,
    -0.7745966692414834,
    0.7745966692414834
  ],
  [
    -0.33998104358485626,
    0.33998104358485626,
    -0.8611363115940526,
    0.8611363115940526
  ],
  [
    0,
    -0.5384693101056831,
    0.5384693101056831,
    -0.906179845938664,
    0.906179845938664
  ],
  [
    0.6612093864662645,
    -0.6612093864662645,
    -0.2386191860831969,
    0.2386191860831969,
    -0.932469514203152,
    0.932469514203152
  ],
  [
    0,
    0.4058451513773972,
    -0.4058451513773972,
    -0.7415311855993945,
    0.7415311855993945,
    -0.9491079123427585,
    0.9491079123427585
  ],
  [
    -0.1834346424956498,
    0.1834346424956498,
    -0.525532409916329,
    0.525532409916329,
    -0.7966664774136267,
    0.7966664774136267,
    -0.9602898564975363,
    0.9602898564975363
  ],
  [
    0,
    -0.8360311073266358,
    0.8360311073266358,
    -0.9681602395076261,
    0.9681602395076261,
    -0.3242534234038089,
    0.3242534234038089,
    -0.6133714327005904,
    0.6133714327005904
  ],
  [
    -0.14887433898163122,
    0.14887433898163122,
    -0.4333953941292472,
    0.4333953941292472,
    -0.6794095682990244,
    0.6794095682990244,
    -0.8650633666889845,
    0.8650633666889845,
    -0.9739065285171717,
    0.9739065285171717
  ],
  [
    0,
    -0.26954315595234496,
    0.26954315595234496,
    -0.5190961292068118,
    0.5190961292068118,
    -0.7301520055740494,
    0.7301520055740494,
    -0.8870625997680953,
    0.8870625997680953,
    -0.978228658146057,
    0.978228658146057
  ],
  [
    -0.1252334085114689,
    0.1252334085114689,
    -0.3678314989981802,
    0.3678314989981802,
    -0.5873179542866175,
    0.5873179542866175,
    -0.7699026741943047,
    0.7699026741943047,
    -0.9041172563704749,
    0.9041172563704749,
    -0.9815606342467192,
    0.9815606342467192
  ],
  [
    0,
    -0.2304583159551348,
    0.2304583159551348,
    -0.44849275103644687,
    0.44849275103644687,
    -0.6423493394403402,
    0.6423493394403402,
    -0.8015780907333099,
    0.8015780907333099,
    -0.9175983992229779,
    0.9175983992229779,
    -0.9841830547185881,
    0.9841830547185881
  ],
  [
    -0.10805494870734367,
    0.10805494870734367,
    -0.31911236892788974,
    0.31911236892788974,
    -0.5152486363581541,
    0.5152486363581541,
    -0.6872929048116855,
    0.6872929048116855,
    -0.827201315069765,
    0.827201315069765,
    -0.9284348836635735,
    0.9284348836635735,
    -0.9862838086968123,
    0.9862838086968123
  ],
  [
    0,
    -0.20119409399743451,
    0.20119409399743451,
    -0.3941513470775634,
    0.3941513470775634,
    -0.5709721726085388,
    0.5709721726085388,
    -0.7244177313601701,
    0.7244177313601701,
    -0.8482065834104272,
    0.8482065834104272,
    -0.937273392400706,
    0.937273392400706,
    -0.9879925180204854,
    0.9879925180204854
  ],
  [
    -0.09501250983763744,
    0.09501250983763744,
    -0.2816035507792589,
    0.2816035507792589,
    -0.45801677765722737,
    0.45801677765722737,
    -0.6178762444026438,
    0.6178762444026438,
    -0.755404408355003,
    0.755404408355003,
    -0.8656312023878318,
    0.8656312023878318,
    -0.9445750230732326,
    0.9445750230732326,
    -0.9894009349916499,
    0.9894009349916499
  ],
  [
    0,
    -0.17848418149584785,
    0.17848418149584785,
    -0.3512317634538763,
    0.3512317634538763,
    -0.5126905370864769,
    0.5126905370864769,
    -0.6576711592166907,
    0.6576711592166907,
    -0.7815140038968014,
    0.7815140038968014,
    -0.8802391537269859,
    0.8802391537269859,
    -0.9506755217687678,
    0.9506755217687678,
    -0.9905754753144174,
    0.9905754753144174
  ],
  [
    -0.0847750130417353,
    0.0847750130417353,
    -0.2518862256915055,
    0.2518862256915055,
    -0.41175116146284263,
    0.41175116146284263,
    -0.5597708310739475,
    0.5597708310739475,
    -0.6916870430603532,
    0.6916870430603532,
    -0.8037049589725231,
    0.8037049589725231,
    -0.8926024664975557,
    0.8926024664975557,
    -0.9558239495713977,
    0.9558239495713977,
    -0.9915651684209309,
    0.9915651684209309
  ],
  [
    0,
    -0.16035864564022537,
    0.16035864564022537,
    -0.31656409996362983,
    0.31656409996362983,
    -0.46457074137596094,
    0.46457074137596094,
    -0.600545304661681,
    0.600545304661681,
    -0.7209661773352294,
    0.7209661773352294,
    -0.8227146565371428,
    0.8227146565371428,
    -0.9031559036148179,
    0.9031559036148179,
    -0.96020815213483,
    0.96020815213483,
    -0.9924068438435844,
    0.9924068438435844
  ],
  [
    -0.07652652113349734,
    0.07652652113349734,
    -0.22778585114164507,
    0.22778585114164507,
    -0.37370608871541955,
    0.37370608871541955,
    -0.5108670019508271,
    0.5108670019508271,
    -0.636053680726515,
    0.636053680726515,
    -0.7463319064601508,
    0.7463319064601508,
    -0.8391169718222188,
    0.8391169718222188,
    -0.912234428251326,
    0.912234428251326,
    -0.9639719272779138,
    0.9639719272779138,
    -0.9931285991850949,
    0.9931285991850949
  ],
  [
    0,
    -0.1455618541608951,
    0.1455618541608951,
    -0.2880213168024011,
    0.2880213168024011,
    -0.4243421202074388,
    0.4243421202074388,
    -0.5516188358872198,
    0.5516188358872198,
    -0.6671388041974123,
    0.6671388041974123,
    -0.7684399634756779,
    0.7684399634756779,
    -0.8533633645833173,
    0.8533633645833173,
    -0.9200993341504008,
    0.9200993341504008,
    -0.9672268385663063,
    0.9672268385663063,
    -0.9937521706203895,
    0.9937521706203895
  ],
  [
    -0.06973927331972223,
    0.06973927331972223,
    -0.20786042668822127,
    0.20786042668822127,
    -0.34193582089208424,
    0.34193582089208424,
    -0.469355837986757,
    0.469355837986757,
    -0.5876404035069116,
    0.5876404035069116,
    -0.6944872631866827,
    0.6944872631866827,
    -0.7878168059792081,
    0.7878168059792081,
    -0.8658125777203002,
    0.8658125777203002,
    -0.926956772187174,
    0.926956772187174,
    -0.9700604978354287,
    0.9700604978354287,
    -0.9942945854823992,
    0.9942945854823992
  ],
  [
    0,
    -0.1332568242984661,
    0.1332568242984661,
    -0.26413568097034495,
    0.26413568097034495,
    -0.3903010380302908,
    0.3903010380302908,
    -0.5095014778460075,
    0.5095014778460075,
    -0.6196098757636461,
    0.6196098757636461,
    -0.7186613631319502,
    0.7186613631319502,
    -0.8048884016188399,
    0.8048884016188399,
    -0.8767523582704416,
    0.8767523582704416,
    -0.9329710868260161,
    0.9329710868260161,
    -0.9725424712181152,
    0.9725424712181152,
    -0.9947693349975522,
    0.9947693349975522
  ],
  [
    -0.06405689286260563,
    0.06405689286260563,
    -0.1911188674736163,
    0.1911188674736163,
    -0.3150426796961634,
    0.3150426796961634,
    -0.4337935076260451,
    0.4337935076260451,
    -0.5454214713888396,
    0.5454214713888396,
    -0.6480936519369755,
    0.6480936519369755,
    -0.7401241915785544,
    0.7401241915785544,
    -0.820001985973903,
    0.820001985973903,
    -0.8864155270044011,
    0.8864155270044011,
    -0.9382745520027328,
    0.9382745520027328,
    -0.9747285559713095,
    0.9747285559713095,
    -0.9951872199970213,
    0.9951872199970213
  ]
];
var cValues = [
  [],
  [],
  [1, 1],
  [
    0.8888888888888888,
    0.5555555555555556,
    0.5555555555555556
  ],
  [
    0.6521451548625461,
    0.6521451548625461,
    0.34785484513745385,
    0.34785484513745385
  ],
  [
    0.5688888888888889,
    0.47862867049936647,
    0.47862867049936647,
    0.23692688505618908,
    0.23692688505618908
  ],
  [
    0.3607615730481386,
    0.3607615730481386,
    0.46791393457269104,
    0.46791393457269104,
    0.17132449237917036,
    0.17132449237917036
  ],
  [
    0.4179591836734694,
    0.3818300505051189,
    0.3818300505051189,
    0.27970539148927664,
    0.27970539148927664,
    0.1294849661688697,
    0.1294849661688697
  ],
  [
    0.362683783378362,
    0.362683783378362,
    0.31370664587788727,
    0.31370664587788727,
    0.22238103445337448,
    0.22238103445337448,
    0.10122853629037626,
    0.10122853629037626
  ],
  [
    0.3302393550012598,
    0.1806481606948574,
    0.1806481606948574,
    0.08127438836157441,
    0.08127438836157441,
    0.31234707704000286,
    0.31234707704000286,
    0.26061069640293544,
    0.26061069640293544
  ],
  [
    0.29552422471475287,
    0.29552422471475287,
    0.26926671930999635,
    0.26926671930999635,
    0.21908636251598204,
    0.21908636251598204,
    0.1494513491505806,
    0.1494513491505806,
    0.06667134430868814,
    0.06667134430868814
  ],
  [
    0.2729250867779006,
    0.26280454451024665,
    0.26280454451024665,
    0.23319376459199048,
    0.23319376459199048,
    0.18629021092773426,
    0.18629021092773426,
    0.1255803694649046,
    0.1255803694649046,
    0.05566856711617366,
    0.05566856711617366
  ],
  [
    0.24914704581340277,
    0.24914704581340277,
    0.2334925365383548,
    0.2334925365383548,
    0.20316742672306592,
    0.20316742672306592,
    0.16007832854334622,
    0.16007832854334622,
    0.10693932599531843,
    0.10693932599531843,
    0.04717533638651183,
    0.04717533638651183
  ],
  [
    0.2325515532308739,
    0.22628318026289723,
    0.22628318026289723,
    0.2078160475368885,
    0.2078160475368885,
    0.17814598076194574,
    0.17814598076194574,
    0.13887351021978725,
    0.13887351021978725,
    0.09212149983772845,
    0.09212149983772845,
    0.04048400476531588,
    0.04048400476531588
  ],
  [
    0.2152638534631578,
    0.2152638534631578,
    0.2051984637212956,
    0.2051984637212956,
    0.18553839747793782,
    0.18553839747793782,
    0.15720316715819355,
    0.15720316715819355,
    0.12151857068790319,
    0.12151857068790319,
    0.08015808715976021,
    0.08015808715976021,
    0.03511946033175186,
    0.03511946033175186
  ],
  [
    0.2025782419255613,
    0.19843148532711158,
    0.19843148532711158,
    0.1861610000155622,
    0.1861610000155622,
    0.16626920581699392,
    0.16626920581699392,
    0.13957067792615432,
    0.13957067792615432,
    0.10715922046717194,
    0.10715922046717194,
    0.07036604748810812,
    0.07036604748810812,
    0.03075324199611727,
    0.03075324199611727
  ],
  [
    0.1894506104550685,
    0.1894506104550685,
    0.18260341504492358,
    0.18260341504492358,
    0.16915651939500254,
    0.16915651939500254,
    0.14959598881657674,
    0.14959598881657674,
    0.12462897125553388,
    0.12462897125553388,
    0.09515851168249279,
    0.09515851168249279,
    0.062253523938647894,
    0.062253523938647894,
    0.027152459411754096,
    0.027152459411754096
  ],
  [
    0.17944647035620653,
    0.17656270536699264,
    0.17656270536699264,
    0.16800410215645004,
    0.16800410215645004,
    0.15404576107681028,
    0.15404576107681028,
    0.13513636846852548,
    0.13513636846852548,
    0.11188384719340397,
    0.11188384719340397,
    0.08503614831717918,
    0.08503614831717918,
    0.0554595293739872,
    0.0554595293739872,
    0.02414830286854793,
    0.02414830286854793
  ],
  [
    0.1691423829631436,
    0.1691423829631436,
    0.16427648374583273,
    0.16427648374583273,
    0.15468467512626524,
    0.15468467512626524,
    0.14064291467065065,
    0.14064291467065065,
    0.12255520671147846,
    0.12255520671147846,
    0.10094204410628717,
    0.10094204410628717,
    0.07642573025488905,
    0.07642573025488905,
    0.0497145488949698,
    0.0497145488949698,
    0.02161601352648331,
    0.02161601352648331
  ],
  [
    0.1610544498487837,
    0.15896884339395434,
    0.15896884339395434,
    0.15276604206585967,
    0.15276604206585967,
    0.1426067021736066,
    0.1426067021736066,
    0.12875396253933621,
    0.12875396253933621,
    0.11156664554733399,
    0.11156664554733399,
    0.09149002162245,
    0.09149002162245,
    0.06904454273764123,
    0.06904454273764123,
    0.0448142267656996,
    0.0448142267656996,
    0.019461788229726478,
    0.019461788229726478
  ],
  [
    0.15275338713072584,
    0.15275338713072584,
    0.14917298647260374,
    0.14917298647260374,
    0.14209610931838204,
    0.14209610931838204,
    0.13168863844917664,
    0.13168863844917664,
    0.11819453196151841,
    0.11819453196151841,
    0.10193011981724044,
    0.10193011981724044,
    0.08327674157670475,
    0.08327674157670475,
    0.06267204833410907,
    0.06267204833410907,
    0.04060142980038694,
    0.04060142980038694,
    0.017614007139152118,
    0.017614007139152118
  ],
  [
    0.14608113364969041,
    0.14452440398997005,
    0.14452440398997005,
    0.13988739479107315,
    0.13988739479107315,
    0.13226893863333747,
    0.13226893863333747,
    0.12183141605372853,
    0.12183141605372853,
    0.10879729916714838,
    0.10879729916714838,
    0.09344442345603386,
    0.09344442345603386,
    0.0761001136283793,
    0.0761001136283793,
    0.057134425426857205,
    0.057134425426857205,
    0.036953789770852494,
    0.036953789770852494,
    0.016017228257774335,
    0.016017228257774335
  ],
  [
    0.13925187285563198,
    0.13925187285563198,
    0.13654149834601517,
    0.13654149834601517,
    0.13117350478706238,
    0.13117350478706238,
    0.12325237681051242,
    0.12325237681051242,
    0.11293229608053922,
    0.11293229608053922,
    0.10041414444288096,
    0.10041414444288096,
    0.08594160621706773,
    0.08594160621706773,
    0.06979646842452049,
    0.06979646842452049,
    0.052293335152683286,
    0.052293335152683286,
    0.03377490158481415,
    0.03377490158481415,
    0.0146279952982722,
    0.0146279952982722
  ],
  [
    0.13365457218610619,
    0.1324620394046966,
    0.1324620394046966,
    0.12890572218808216,
    0.12890572218808216,
    0.12304908430672953,
    0.12304908430672953,
    0.11499664022241136,
    0.11499664022241136,
    0.10489209146454141,
    0.10489209146454141,
    0.09291576606003515,
    0.09291576606003515,
    0.07928141177671895,
    0.07928141177671895,
    0.06423242140852585,
    0.06423242140852585,
    0.04803767173108467,
    0.04803767173108467,
    0.030988005856979445,
    0.030988005856979445,
    0.013411859487141771,
    0.013411859487141771
  ],
  [
    0.12793819534675216,
    0.12793819534675216,
    0.1258374563468283,
    0.1258374563468283,
    0.12167047292780339,
    0.12167047292780339,
    0.1155056680537256,
    0.1155056680537256,
    0.10744427011596563,
    0.10744427011596563,
    0.09761865210411388,
    0.09761865210411388,
    0.08619016153195327,
    0.08619016153195327,
    0.0733464814110803,
    0.0733464814110803,
    0.05929858491543678,
    0.05929858491543678,
    0.04427743881741981,
    0.04427743881741981,
    0.028531388628933663,
    0.028531388628933663,
    0.0123412297999872,
    0.0123412297999872
  ]
];
var binomialCoefficients = [[1], [1, 1], [1, 2, 1], [1, 3, 3, 1]];
var cubicPoint = (xs, ys, t) => {
  const x = (1 - t) * (1 - t) * (1 - t) * xs[0] + 3 * (1 - t) * (1 - t) * t * xs[1] + 3 * (1 - t) * t * t * xs[2] + t * t * t * xs[3];
  const y = (1 - t) * (1 - t) * (1 - t) * ys[0] + 3 * (1 - t) * (1 - t) * t * ys[1] + 3 * (1 - t) * t * t * ys[2] + t * t * t * ys[3];
  return { x, y };
};
var getDerivative = (derivative, t, vs) => {
  const n = vs.length - 1;
  let value;
  if (n === 0) {
    return 0;
  }
  if (derivative === 0) {
    value = 0;
    for (let k = 0; k <= n; k++) {
      value += binomialCoefficients[n][k] * (1 - t) ** (n - k) * t ** k * vs[k];
    }
    return value;
  }
  const _vs = new Array(n);
  for (let k = 0; k < n; k++) {
    _vs[k] = n * (vs[k + 1] - vs[k]);
  }
  return getDerivative(derivative - 1, t, _vs);
};
function bFunc(xs, ys, t) {
  const xbase = getDerivative(1, t, xs);
  const ybase = getDerivative(1, t, ys);
  const combined = xbase * xbase + ybase * ybase;
  return Math.sqrt(combined);
}
var getCubicArcLength = ({
  sx,
  sy,
  t
}) => {
  let correctedT;
  const n = 20;
  const z = t / 2;
  let sum = 0;
  for (let i2 = 0; i2 < n; i2++) {
    correctedT = z * tValues[n][i2] + z;
    sum += cValues[n][i2] * bFunc(sx, sy, correctedT);
  }
  return z * sum;
};
var quadraticPoint = (xs, ys, t) => {
  const x = (1 - t) * (1 - t) * xs[0] + 2 * (1 - t) * t * xs[1] + t * t * xs[2];
  const y = (1 - t) * (1 - t) * ys[0] + 2 * (1 - t) * t * ys[1] + t * t * ys[2];
  return { x, y };
};
var cubicDerivative = (xs, ys, t) => {
  const derivative = quadraticPoint([3 * (xs[1] - xs[0]), 3 * (xs[2] - xs[1]), 3 * (xs[3] - xs[2])], [3 * (ys[1] - ys[0]), 3 * (ys[2] - ys[1]), 3 * (ys[3] - ys[2])], t);
  return derivative;
};
var t2length = ({
  length: length2,
  totalLength,
  func
}) => {
  let error = 1;
  let t = length2 / totalLength;
  let step = (length2 - func(t)) / totalLength;
  let numIterations = 0;
  while (error > 1e-3) {
    const increasedTLength = func(t + step);
    const increasedTError = Math.abs(length2 - increasedTLength) / totalLength;
    if (increasedTError < error) {
      error = increasedTError;
      t += step;
    } else {
      const decreasedTLength = func(t - step);
      const decreasedTError = Math.abs(length2 - decreasedTLength) / totalLength;
      if (decreasedTError < error) {
        error = decreasedTError;
        t -= step;
      } else {
        step /= 2;
      }
    }
    numIterations++;
    if (numIterations > 500) {
      break;
    }
  }
  return t;
};
var makeCubic = ({
  startX,
  startY,
  cp1x,
  cp1y,
  cp2x,
  cp2y,
  x,
  y
}) => {
  const a = { x: startX, y: startY };
  const b = { x: cp1x, y: cp1y };
  const c = { x: cp2x, y: cp2y };
  const d = { x, y };
  const length2 = getCubicArcLength({
    sx: [a.x, b.x, c.x, d.x],
    sy: [a.y, b.y, c.y, d.y],
    t: 1
  });
  const getTotalLength = () => {
    return length2;
  };
  const getPointAtLength = (len) => {
    const sx = [a.x, b.x, c.x, d.x];
    const sy = [a.y, b.y, c.y, d.y];
    const t = t2length({
      length: len,
      totalLength: length2,
      func: (i2) => {
        return getCubicArcLength({ sx, sy, t: i2 });
      }
    });
    return cubicPoint(sx, sy, t);
  };
  const getTangentAtLength = (len) => {
    const xs = [a.x, b.x, c.x, d.x];
    const xy = [a.y, b.y, c.y, d.y];
    const t = t2length({
      length: len,
      totalLength: length2,
      func: (i2) => getCubicArcLength({ sx: xs, sy: xy, t: i2 })
    });
    const derivative = cubicDerivative(xs, xy, t);
    const mdl = Math.sqrt(derivative.x * derivative.x + derivative.y * derivative.y);
    let tangent;
    if (mdl > 0) {
      tangent = { x: derivative.x / mdl, y: derivative.y / mdl };
    } else {
      tangent = { x: 0, y: 0 };
    }
    return tangent;
  };
  const getC = () => {
    return c;
  };
  const getD = () => {
    return d;
  };
  return {
    getPointAtLength,
    getTangentAtLength,
    getTotalLength,
    getC,
    getD,
    type: "cubic-bezier"
  };
};
var makeLinearPosition = ({
  x0,
  x1,
  y0,
  y1
}) => {
  return {
    getTotalLength: () => {
      return Math.sqrt((x0 - x1) ** 2 + (y0 - y1) ** 2);
    },
    getPointAtLength: (pos) => {
      let fraction = pos / Math.sqrt((x0 - x1) ** 2 + (y0 - y1) ** 2);
      fraction = Number.isNaN(fraction) ? 1 : fraction;
      const newDeltaX = (x1 - x0) * fraction;
      const newDeltaY = (y1 - y0) * fraction;
      return { x: x0 + newDeltaX, y: y0 + newDeltaY };
    },
    getTangentAtLength: () => {
      const module = Math.sqrt((x1 - x0) * (x1 - x0) + (y1 - y0) * (y1 - y0));
      return { x: (x1 - x0) / module, y: (y1 - y0) / module };
    },
    type: "linear"
  };
};
var conductAnalysis = (instructions) => {
  let currentPoint = { x: 0, y: 0 };
  let moveStart = { x: 0, y: 0 };
  const segments = [];
  for (let i2 = 0; i2 < instructions.length; i2++) {
    const instruction = instructions[i2];
    if (instruction.type === "M") {
      currentPoint = { x: instruction.x, y: instruction.y };
      moveStart = { x: currentPoint.x, y: currentPoint.y };
      segments.push({
        startPoint: { x: instruction.x, y: instruction.y },
        instructionsAndInfo: [
          {
            instruction,
            function: null,
            length: 0,
            startPoint: currentPoint
          }
        ]
      });
    }
    if (instruction.type === "L") {
      if (segments.length > 0) {
        const length2 = Math.sqrt((currentPoint.x - instruction.x) ** 2 + (currentPoint.y - instruction.y) ** 2);
        segments[segments.length - 1].instructionsAndInfo.push({
          instruction,
          length: length2,
          function: makeLinearPosition({
            x0: currentPoint.x,
            x1: instruction.x,
            y0: currentPoint.y,
            y1: instruction.y
          }),
          startPoint: currentPoint
        });
      }
      currentPoint = { x: instruction.x, y: instruction.y };
    }
    if (instruction.type === "Z") {
      if (segments.length > 0) {
        const length2 = Math.sqrt((segments[segments.length - 1].startPoint.x - currentPoint.x) ** 2 + (segments[segments.length - 1].startPoint.y - currentPoint.y) ** 2);
        segments[segments.length - 1].instructionsAndInfo.push({
          instruction,
          function: makeLinearPosition({
            x0: currentPoint.x,
            x1: moveStart.x,
            y0: currentPoint.y,
            y1: moveStart.y
          }),
          length: length2,
          startPoint: { ...currentPoint }
        });
      }
      currentPoint = { x: moveStart.x, y: moveStart.y };
    }
    if (instruction.type === "C") {
      const curve = makeCubic({
        startX: currentPoint.x,
        startY: currentPoint.y,
        cp1x: instruction.cp1x,
        cp1y: instruction.cp1y,
        cp2x: instruction.cp2x,
        cp2y: instruction.cp2y,
        x: instruction.x,
        y: instruction.y
      });
      const length2 = curve.getTotalLength();
      if (segments.length > 0) {
        segments[segments.length - 1].instructionsAndInfo.push({
          instruction,
          length: length2,
          function: curve,
          startPoint: { ...currentPoint }
        });
      }
      currentPoint = { x: instruction.x, y: instruction.y };
    }
  }
  return segments;
};
var length = {
  a: 7,
  A: 7,
  C: 6,
  c: 6,
  H: 1,
  h: 1,
  L: 2,
  l: 2,
  M: 2,
  m: 2,
  Q: 4,
  q: 4,
  S: 4,
  s: 4,
  T: 2,
  t: 2,
  V: 1,
  v: 1,
  Z: 0,
  z: 0
};
var chunkExact = (array, instruction) => {
  const chunks = [];
  const expectedSize = length[instruction];
  if (array.length % expectedSize !== 0) {
    throw new Error(`Expected number of arguments of SVG instruction "${instruction} ${array.join(" ")}" to be a multiple of ${expectedSize}`);
  }
  for (let i2 = 0; i2 < array.length; i2 += expectedSize) {
    chunks.push(array.slice(i2, i2 + expectedSize));
  }
  return chunks;
};
var makeInstructions = (arr, instruction, cb) => {
  return chunkExact(arr, instruction).map((args) => {
    return cb(args);
  });
};
var segmentRegExp = /([astvzqmhlc])([^astvzqmhlc]*)/gi;
var numberRegExp = /-?[0-9]*\.?[0-9]+(?:e[-+]?\d+)?/gi;
var parseValues = (args, instructionType) => {
  const numbers = args.match(numberRegExp);
  if (!numbers) {
    if (instructionType === "Z" || instructionType === "z") {
      return [];
    }
    throw new Error(`Malformed path data: ${instructionType} was expected to have numbers afterwards`);
  }
  const expectedArguments = length[instructionType];
  if (numbers.length % expectedArguments !== 0) {
    throw new Error(`Malformed path data: ${instructionType} was expected to have a multiple of ${expectedArguments} numbers, but got "${instructionType} ${numbers.join(" ")} instead"`);
  }
  return numbers.map(Number);
};
var parsePath = (path) => {
  if (!path) {
    throw new Error("No path provided");
  }
  const segments = path.match(segmentRegExp);
  if (!segments) {
    throw new Error(`No path elements found in string ${path}`);
  }
  return segments.map((segmentString) => {
    const command = segmentString.charAt(0);
    const args = parseValues(segmentString.substring(1), command);
    if (command === "M" && args.length > 2) {
      const segmentsArray = [];
      segmentsArray.push({
        type: command,
        x: args[0],
        y: args[1]
      });
      segmentsArray.push(...makeInstructions(args.slice(2), "L", (numbers) => ({
        type: "L",
        x: numbers[0],
        y: numbers[1]
      })));
      return segmentsArray;
    }
    if (command === "m" && args.length > 2) {
      const segmentsArray = [];
      segmentsArray.push({
        type: command,
        dx: args[0],
        dy: args[1]
      });
      segmentsArray.push(...makeInstructions(args.slice(2), "l", (numbers) => ({
        type: "l",
        dx: numbers[0],
        dy: numbers[1]
      })));
      return segmentsArray;
    }
    if (command === "Z" || command === "z") {
      return [
        {
          type: "Z"
        }
      ];
    }
    if (command === "A") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        rx: numbers[0],
        ry: numbers[1],
        xAxisRotation: numbers[2],
        largeArcFlag: numbers[3] === 1,
        sweepFlag: numbers[4] === 1,
        x: numbers[5],
        y: numbers[6]
      }));
    }
    if (command === "a") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        rx: numbers[0],
        ry: numbers[1],
        xAxisRotation: numbers[2],
        largeArcFlag: numbers[3] === 1,
        sweepFlag: numbers[4] === 1,
        dx: numbers[5],
        dy: numbers[6]
      }));
    }
    if (command === "C") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        cp1x: numbers[0],
        cp1y: numbers[1],
        cp2x: numbers[2],
        cp2y: numbers[3],
        x: numbers[4],
        y: numbers[5]
      }));
    }
    if (command === "c") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        cp1dx: numbers[0],
        cp1dy: numbers[1],
        cp2dx: numbers[2],
        cp2dy: numbers[3],
        dx: numbers[4],
        dy: numbers[5]
      }));
    }
    if (command === "S") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        cpx: numbers[0],
        cpy: numbers[1],
        x: numbers[2],
        y: numbers[3]
      }));
    }
    if (command === "s") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        cpdx: numbers[0],
        cpdy: numbers[1],
        dx: numbers[2],
        dy: numbers[3]
      }));
    }
    if (command === "H") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        x: numbers[0]
      }));
    }
    if (command === "h") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        dx: numbers[0]
      }));
    }
    if (command === "V") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        y: numbers[0]
      }));
    }
    if (command === "v") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        dy: numbers[0]
      }));
    }
    if (command === "L") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        x: numbers[0],
        y: numbers[1]
      }));
    }
    if (command === "M") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        x: numbers[0],
        y: numbers[1]
      }));
    }
    if (command === "m") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        dx: numbers[0],
        dy: numbers[1]
      }));
    }
    if (command === "l") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        dx: numbers[0],
        dy: numbers[1]
      }));
    }
    if (command === "Q") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        cpx: numbers[0],
        cpy: numbers[1],
        x: numbers[2],
        y: numbers[3]
      }));
    }
    if (command === "q") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        cpdx: numbers[0],
        cpdy: numbers[1],
        dx: numbers[2],
        dy: numbers[3]
      }));
    }
    if (command === "T") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        x: numbers[0],
        y: numbers[1]
      }));
    }
    if (command === "t") {
      return makeInstructions(args, command, (numbers) => ({
        type: command,
        dx: numbers[0],
        dy: numbers[1]
      }));
    }
    throw new Error(`Invalid path element ${segmentString}`);
  }, []).flat(1);
};
var convertQToCInstruction = (instruction, startPoint) => {
  const cp1x = startPoint.x + 2 / 3 * (instruction.cpx - startPoint.x);
  const cp1y = startPoint.y + 2 / 3 * (instruction.cpy - startPoint.y);
  const cp2x = instruction.x + 2 / 3 * (instruction.cpx - instruction.x);
  const cp2y = instruction.y + 2 / 3 * (instruction.cpy - instruction.y);
  return {
    type: "C",
    cp1x,
    cp1y,
    cp2x,
    cp2y,
    x: instruction.x,
    y: instruction.y
  };
};
var iterateOverSegments = ({
  segments,
  iterate
}) => {
  let x = 0;
  let y = 0;
  let initialX = 0;
  let initialY = 0;
  let cpX = null;
  let cpY = null;
  const newSegments = segments.map((s, i2) => {
    const newSeg = iterate({
      segment: s,
      x,
      y,
      prevSegment: segments[i2 - 1] ?? null,
      initialX,
      initialY,
      cpX,
      cpY
    });
    switch (s.type) {
      case "M":
        initialX = s.x;
        initialY = s.y;
        x = s.x;
        y = s.y;
        cpX = null;
        cpY = null;
        break;
      case "Q":
        x = s.x;
        y = s.y;
        cpX = s.cpx;
        cpY = s.cpy;
        break;
      case "A":
        x = s.x;
        y = s.y;
        cpX = null;
        cpY = null;
        break;
      case "C":
        x = s.x;
        y = s.y;
        cpX = s.cp2x;
        cpY = s.cp2y;
        break;
      case "S":
        x = s.x;
        y = s.y;
        cpX = s.cpx;
        cpY = s.cpy;
        break;
      case "T":
        if (cpX !== null && cpY !== null) {
          cpX = x - (cpX - x);
          cpY = y - (cpY - y);
        }
        x = s.x;
        y = s.y;
        break;
      case "L":
        x = s.x;
        y = s.y;
        cpX = null;
        cpY = null;
        break;
      case "V":
        y = s.y;
        cpX = null;
        cpY = null;
        break;
      case "H":
        x = s.x;
        cpX = null;
        cpY = null;
        break;
      case "Z":
        x = initialX;
        y = initialY;
        cpX = null;
        cpY = null;
        break;
      default:
        throw new Error(`Unexpected instruction ${s.type}`);
    }
    return newSeg;
  });
  return newSegments.flat(1);
};
var TAU = Math.PI * 2;
function approximate_unit_arc(theta1, delta_theta) {
  const alpha = 4 / 3 * Math.tan(delta_theta / 4);
  const x1 = Math.cos(theta1);
  const y1 = Math.sin(theta1);
  const x2 = Math.cos(theta1 + delta_theta);
  const y2 = Math.sin(theta1 + delta_theta);
  return [
    x1,
    y1,
    x1 - y1 * alpha,
    y1 + x1 * alpha,
    x2 + y2 * alpha,
    y2 - x2 * alpha,
    x2,
    y2
  ];
}
function unit_vector_angle(ux, uy, vx, vy) {
  const sign = ux * vy - uy * vx < 0 ? -1 : 1;
  let dot = ux * vx + uy * vy;
  if (dot > 1) {
    dot = 1;
  }
  if (dot < -1) {
    dot = -1;
  }
  return sign * Math.acos(dot);
}
function get_arc_center({
  x1,
  y1,
  x2,
  y2,
  largeArcFlag,
  sweepFlag,
  rx,
  ry,
  sin_phi,
  cos_phi
}) {
  const x1p = cos_phi * (x1 - x2) / 2 + sin_phi * (y1 - y2) / 2;
  const y1p = -sin_phi * (x1 - x2) / 2 + cos_phi * (y1 - y2) / 2;
  const rx_sq = rx * rx;
  const ry_sq = ry * ry;
  const x1p_sq = x1p * x1p;
  const y1p_sq = y1p * y1p;
  let radicant = rx_sq * ry_sq - rx_sq * y1p_sq - ry_sq * x1p_sq;
  if (radicant < 0) {
    radicant = 0;
  }
  radicant /= rx_sq * y1p_sq + ry_sq * x1p_sq;
  radicant = Math.sqrt(radicant) * (largeArcFlag === sweepFlag ? -1 : 1);
  const cxp = radicant * rx / ry * y1p;
  const cyp = radicant * -ry / rx * x1p;
  const cx = cos_phi * cxp - sin_phi * cyp + (x1 + x2) / 2;
  const cy = sin_phi * cxp + cos_phi * cyp + (y1 + y2) / 2;
  const v1x = (x1p - cxp) / rx;
  const v1y = (y1p - cyp) / ry;
  const v2x = (-x1p - cxp) / rx;
  const v2y = (-y1p - cyp) / ry;
  const theta1 = unit_vector_angle(1, 0, v1x, v1y);
  let delta_theta = unit_vector_angle(v1x, v1y, v2x, v2y);
  if (sweepFlag === false && delta_theta > 0) {
    delta_theta -= TAU;
  }
  if (sweepFlag === true && delta_theta < 0) {
    delta_theta += TAU;
  }
  return [cx, cy, theta1, delta_theta];
}
function arcToCircle({
  x1,
  y1,
  x2,
  y2,
  largeArcFlag,
  sweepFlag,
  rx,
  ry,
  phi
}) {
  const sin_phi = Math.sin(phi * TAU / 360);
  const cos_phi = Math.cos(phi * TAU / 360);
  const x1p = cos_phi * (x1 - x2) / 2 + sin_phi * (y1 - y2) / 2;
  const y1p = -sin_phi * (x1 - x2) / 2 + cos_phi * (y1 - y2) / 2;
  if (x1p === 0 && y1p === 0) {
    return [];
  }
  if (rx === 0 || ry === 0) {
    return [];
  }
  rx = Math.abs(rx);
  ry = Math.abs(ry);
  const lambda = x1p * x1p / (rx * rx) + y1p * y1p / (ry * ry);
  if (lambda > 1) {
    rx *= Math.sqrt(lambda);
    ry *= Math.sqrt(lambda);
  }
  const cc = get_arc_center({
    x1,
    y1,
    x2,
    y2,
    largeArcFlag,
    sweepFlag,
    rx,
    ry,
    sin_phi,
    cos_phi
  });
  const result = [];
  let theta1 = cc[2];
  let delta_theta = cc[3];
  const segments = Math.max(Math.ceil(Math.abs(delta_theta) / (TAU / 4)), 1);
  delta_theta /= segments;
  for (let i2 = 0; i2 < segments; i2++) {
    result.push(approximate_unit_arc(theta1, delta_theta));
    theta1 += delta_theta;
  }
  return result.map((curve) => {
    for (let i2 = 0; i2 < curve.length; i2 += 2) {
      let x = curve[i2 + 0];
      let y = curve[i2 + 1];
      x *= rx;
      y *= ry;
      const xp = cos_phi * x - sin_phi * y;
      const yp = sin_phi * x + cos_phi * y;
      curve[i2 + 0] = xp + cc[0];
      curve[i2 + 1] = yp + cc[1];
    }
    return curve;
  });
}
var removeATSHVQInstructions = (segments) => {
  return iterateOverSegments({
    segments,
    iterate: ({ segment, prevSegment, x, y, cpX, cpY }) => {
      if (segment.type === "H") {
        return [{ type: "L", x: segment.x, y }];
      }
      if (segment.type === "V") {
        return [{ type: "L", x, y: segment.y }];
      }
      if (segment.type === "A") {
        const nextX = segment.x;
        const nextY = segment.y;
        const new_segments = arcToCircle({
          x1: x,
          y1: y,
          x2: nextX,
          y2: nextY,
          largeArcFlag: segment.largeArcFlag,
          sweepFlag: segment.sweepFlag,
          rx: segment.rx,
          ry: segment.ry,
          phi: segment.xAxisRotation
        });
        if (new_segments.length === 0) {
          return [
            {
              type: "L",
              x: segment.x,
              y: segment.y
            }
          ];
        }
        const result = new_segments.map((_s) => {
          return {
            type: "C",
            cp1x: _s[2],
            cp1y: _s[3],
            cp2x: _s[4],
            cp2y: _s[5],
            x: _s[6],
            y: _s[7]
          };
        });
        return result;
      }
      if (segment.type === "T") {
        let prevControlX = 0;
        let prevControlY = 0;
        if (prevSegment && (prevSegment.type === "Q" || prevSegment.type === "T")) {
          prevControlX = cpX;
          prevControlY = cpY;
        } else {
          prevControlX = x;
          prevControlY = y;
        }
        const vectorX = prevControlX - x;
        const vectorY = prevControlY - y;
        const newControlX = x - vectorX;
        const newControlY = y - vectorY;
        return [
          convertQToCInstruction({
            type: "Q",
            cpx: newControlX,
            cpy: newControlY,
            x: segment.x,
            y: segment.y
          }, { x, y })
        ];
      }
      if (segment.type === "S") {
        let prevControlX = 0;
        let prevControlY = 0;
        if (prevSegment && prevSegment.type === "C") {
          prevControlX = prevSegment.cp2x;
          prevControlY = prevSegment.cp2y;
        } else if (prevSegment && prevSegment.type === "S") {
          prevControlX = prevSegment.cpx;
          prevControlY = prevSegment.cpy;
        } else {
          prevControlX = x;
          prevControlY = y;
        }
        const vectorX = prevControlX - x;
        const vectorY = prevControlY - y;
        const newControlX = x - vectorX;
        const newControlY = y - vectorY;
        return [
          {
            type: "C",
            cp1x: newControlX,
            cp1y: newControlY,
            cp2x: segment.cpx,
            cp2y: segment.cpy,
            x: segment.x,
            y: segment.y
          }
        ];
      }
      if (segment.type === "Q") {
        return [convertQToCInstruction(segment, { x, y })];
      }
      return [segment];
    }
  });
};
var serializeInstruction = (instruction) => {
  if (instruction.type === "A") {
    return `A ${instruction.rx} ${instruction.ry} ${instruction.xAxisRotation} ${Number(instruction.largeArcFlag)} ${Number(instruction.sweepFlag)} ${instruction.x} ${instruction.y}`;
  }
  if (instruction.type === "a") {
    return `a ${instruction.rx} ${instruction.ry} ${instruction.xAxisRotation} ${Number(instruction.largeArcFlag)} ${Number(instruction.sweepFlag)} ${instruction.dx} ${instruction.dy}`;
  }
  if (instruction.type === "C") {
    return `C ${instruction.cp1x} ${instruction.cp1y} ${instruction.cp2x} ${instruction.cp2y} ${instruction.x} ${instruction.y}`;
  }
  if (instruction.type === "c") {
    return `c ${instruction.cp1dx} ${instruction.cp1dy} ${instruction.cp2dx} ${instruction.cp2dy} ${instruction.dx} ${instruction.dy}`;
  }
  if (instruction.type === "S") {
    return `S ${instruction.cpx} ${instruction.cpy} ${instruction.x} ${instruction.y}`;
  }
  if (instruction.type === "s") {
    return `s ${instruction.cpdx} ${instruction.cpdy} ${instruction.dx} ${instruction.dy}`;
  }
  if (instruction.type === "Q") {
    return `Q ${instruction.cpx} ${instruction.cpy} ${instruction.x} ${instruction.y}`;
  }
  if (instruction.type === "q") {
    return `q ${instruction.cpdx} ${instruction.cpdy} ${instruction.dx} ${instruction.dy}`;
  }
  if (instruction.type === "Z") {
    return "Z";
  }
  if (instruction.type === "H") {
    return `H ${instruction.x}`;
  }
  if (instruction.type === "h") {
    return `h ${instruction.dx}`;
  }
  if (instruction.type === "V") {
    return `V ${instruction.y}`;
  }
  if (instruction.type === "v") {
    return `v ${instruction.dy}`;
  }
  if (instruction.type === "L") {
    return `L ${instruction.x} ${instruction.y}`;
  }
  if (instruction.type === "l") {
    return `l ${instruction.dx} ${instruction.dy}`;
  }
  if (instruction.type === "M") {
    return `M ${instruction.x} ${instruction.y}`;
  }
  if (instruction.type === "m") {
    return `m ${instruction.dx} ${instruction.dy}`;
  }
  if (instruction.type === "T") {
    return `T ${instruction.x} ${instruction.y}`;
  }
  if (instruction.type === "t") {
    return `t ${instruction.dx} ${instruction.dy}`;
  }
  throw new Error(`Unknown instruction type: ${instruction.type}`);
};
var serializeInstructions = (path) => {
  return path.map((p) => {
    return serializeInstruction(p);
  }).join(" ");
};
var normalizeInstructions = (instructions) => {
  const normalized = [];
  let x = 0;
  let y = 0;
  let moveX = 0;
  let moveY = 0;
  for (let i2 = 0; i2 < instructions.length; i2++) {
    const instruction = instructions[i2];
    if (instruction.type === "M") {
      moveX = instruction.x;
      moveY = instruction.y;
    } else if (instruction.type === "m") {
      moveX += instruction.dx;
      moveY += instruction.dy;
    }
    if (instruction.type === "A" || instruction.type === "C" || instruction.type === "L" || instruction.type === "M" || instruction.type === "Q" || instruction.type === "S" || instruction.type === "T") {
      normalized.push(instruction);
      x = instruction.x;
      y = instruction.y;
      continue;
    }
    if (instruction.type === "a" || instruction.type === "c" || instruction.type === "l" || instruction.type === "m" || instruction.type === "q" || instruction.type === "s" || instruction.type === "t") {
      const currentX = x;
      const currentY = y;
      x += instruction.dx;
      y += instruction.dy;
      if (instruction.type === "a") {
        normalized.push({
          type: "A",
          largeArcFlag: instruction.largeArcFlag,
          rx: instruction.rx,
          ry: instruction.ry,
          sweepFlag: instruction.sweepFlag,
          xAxisRotation: instruction.xAxisRotation,
          x,
          y
        });
        continue;
      }
      if (instruction.type === "c") {
        normalized.push({
          type: "C",
          cp1x: instruction.cp1dx + currentX,
          cp1y: instruction.cp1dy + currentY,
          cp2x: instruction.cp2dx + currentX,
          cp2y: instruction.cp2dy + currentY,
          x,
          y
        });
        continue;
      }
      if (instruction.type === "l") {
        normalized.push({
          type: "L",
          x,
          y
        });
        continue;
      }
      if (instruction.type === "m") {
        normalized.push({
          type: "M",
          x,
          y
        });
        continue;
      }
      if (instruction.type === "q") {
        normalized.push({
          type: "Q",
          cpx: instruction.cpdx + currentX,
          cpy: instruction.cpdy + currentY,
          x,
          y
        });
        continue;
      }
      if (instruction.type === "s") {
        normalized.push({
          type: "S",
          cpx: instruction.cpdx + currentX,
          cpy: instruction.cpdy + currentY,
          x,
          y
        });
        continue;
      }
      if (instruction.type === "t") {
        normalized.push({
          type: "T",
          x,
          y
        });
        continue;
      }
    }
    if (instruction.type === "H") {
      normalized.push(instruction);
      x = instruction.x;
      continue;
    }
    if (instruction.type === "V") {
      normalized.push(instruction);
      y = instruction.y;
      continue;
    }
    if (instruction.type === "Z") {
      normalized.push(instruction);
      x = moveX;
      y = moveY;
      continue;
    }
    if (instruction.type === "h") {
      x += instruction.dx;
      normalized.push({
        type: "H",
        x
      });
      continue;
    }
    if (instruction.type === "v") {
      y += instruction.dy;
      normalized.push({
        type: "V",
        y
      });
      continue;
    }
    throw new Error("Unknown instruction type: " + instruction.type);
  }
  return normalized;
};
var reduceInstructions = (instruction) => {
  const simplified = normalizeInstructions(instruction);
  return removeATSHVQInstructions(simplified);
};
var cutPath = (d, length2) => {
  const parsed = parsePath(d);
  const reduced = reduceInstructions(parsed);
  const constructed = conductAnalysis(reduced);
  const newInstructions = [];
  let summedUpLength = 0;
  for (const segment of constructed) {
    for (const instructionAndInfo of segment.instructionsAndInfo) {
      if (summedUpLength + instructionAndInfo.length > length2) {
        const remainingLength = length2 - summedUpLength;
        const progress = remainingLength / instructionAndInfo.length;
        const cut = cutInstruction({
          instruction: instructionAndInfo.instruction,
          lastPoint: instructionAndInfo.startPoint,
          progress
        });
        newInstructions.push(cut);
        return serializeInstructions(newInstructions);
      }
      summedUpLength += instructionAndInfo.length;
      newInstructions.push(instructionAndInfo.instruction);
      if (summedUpLength === length2) {
        return serializeInstructions(newInstructions);
      }
    }
  }
  return serializeInstructions(newInstructions);
};
var debugPath = (d) => {
  const instructions = normalizeInstructions(parsePath(d));
  return instructions.map((inst, i2) => {
    if (inst.type === "Z") {
      return null;
    }
    if (inst.type === "H" || inst.type === "V") {
      return null;
    }
    const topLeft = [inst.x - 5, inst.y - 5];
    const topRight = [inst.x + 5, inst.y - 5];
    const bottomLeft = [inst.x - 5, inst.y + 5];
    const bottomRight = [inst.x + 5, inst.y + 5];
    const triangle = [
      {
        type: "M",
        x: topLeft[0],
        y: topLeft[1]
      },
      {
        type: "L",
        x: topRight[0],
        y: topRight[1]
      },
      {
        type: "L",
        x: bottomRight[0],
        y: bottomRight[1]
      },
      {
        type: "L",
        x: bottomLeft[0],
        y: bottomLeft[1]
      },
      {
        type: "Z"
      }
    ];
    return {
      d: serializeInstructions(triangle),
      color: i2 === instructions.length - 1 ? "red" : inst.type === "M" ? "blue" : "green"
    };
  }).filter(Boolean);
};
var CBEZIER_MINMAX_EPSILON = 1e-8;
function minmaxQ(A2) {
  const min = Math.min(A2[0], A2[2]);
  const max = Math.max(A2[0], A2[2]);
  if (A2[1] > A2[0] ? A2[2] >= A2[1] : A2[2] <= A2[1]) {
    return [min, max];
  }
  const E = (A2[0] * A2[2] - A2[1] * A2[1]) / (A2[0] - 2 * A2[1] + A2[2]);
  return E < min ? [E, max] : [min, E];
}
function minmaxC(A2) {
  const K = A2[0] - 3 * A2[1] + 3 * A2[2] - A2[3];
  if (Math.abs(K) < CBEZIER_MINMAX_EPSILON) {
    if (A2[0] === A2[3] && A2[0] === A2[1]) {
      return [A2[0], A2[3]];
    }
    return minmaxQ([
      A2[0],
      -0.5 * A2[0] + 1.5 * A2[1],
      A2[0] - 3 * A2[1] + 3 * A2[2]
    ]);
  }
  const T = -A2[0] * A2[2] + A2[0] * A2[3] - A2[1] * A2[2] - A2[1] * A2[3] + A2[1] * A2[1] + A2[2] * A2[2];
  if (T <= 0) {
    return [Math.min(A2[0], A2[3]), Math.max(A2[0], A2[3])];
  }
  const S = Math.sqrt(T);
  let min = Math.min(A2[0], A2[3]);
  let max = Math.max(A2[0], A2[3]);
  const L = A2[0] - 2 * A2[1] + A2[2];
  for (let R = (L + S) / K, i2 = 1; i2 <= 2; R = (L - S) / K, i2++) {
    if (R > 0 && R < 1) {
      const Q2 = A2[0] * (1 - R) * (1 - R) * (1 - R) + A2[1] * 3 * (1 - R) * (1 - R) * R + A2[2] * 3 * (1 - R) * R * R + A2[3] * R * R * R;
      if (Q2 < min) {
        min = Q2;
      }
      if (Q2 > max) {
        max = Q2;
      }
    }
  }
  return [min, max];
}
var getBoundingBoxFromInstructions = (instructions) => {
  let minX = Infinity;
  let minY = Infinity;
  let maxX = -Infinity;
  let maxY = -Infinity;
  let x = 0;
  let y = 0;
  let lastMoveX = 0;
  let lastMoveY = 0;
  for (const seg of instructions) {
    switch (seg.type) {
      case "M": {
        lastMoveX = seg.x;
        lastMoveY = seg.y;
        if (minX > seg.x) {
          minX = seg.x;
        }
        if (minY > seg.y) {
          minY = seg.y;
        }
        if (maxX < seg.x) {
          maxX = seg.x;
        }
        if (maxY < seg.y) {
          maxY = seg.y;
        }
        x = seg.x;
        y = seg.y;
        break;
      }
      case "L": {
        if (minX > seg.x) {
          minX = seg.x;
        }
        if (minY > seg.y) {
          minY = seg.y;
        }
        if (maxX < seg.x) {
          maxX = seg.x;
        }
        if (maxY < seg.y) {
          maxY = seg.y;
        }
        x = seg.x;
        y = seg.y;
        break;
      }
      case "C": {
        const cxMinMax = minmaxC([x, seg.cp1x, seg.cp2x, seg.x]);
        if (minX > cxMinMax[0]) {
          minX = cxMinMax[0];
        }
        if (maxX < cxMinMax[1]) {
          maxX = cxMinMax[1];
        }
        const cyMinMax = minmaxC([y, seg.cp1y, seg.cp2y, seg.y]);
        if (minY > cyMinMax[0]) {
          minY = cyMinMax[0];
        }
        if (maxY < cyMinMax[1]) {
          maxY = cyMinMax[1];
        }
        x = seg.x;
        y = seg.y;
        break;
      }
      case "Z":
        x = lastMoveX;
        y = lastMoveY;
        break;
      default:
        throw new Error(`Unknown instruction ${seg.type}`);
    }
  }
  return {
    x1: minX,
    y1: minY,
    x2: maxX,
    y2: maxY,
    viewBox: `${minX} ${minY} ${maxX - minX} ${maxY - minY}`,
    width: maxX - minX,
    height: maxY - minY
  };
};
var getBoundingBox = (d) => {
  const parsed = parsePath(d);
  const unarced = removeATSHVQInstructions(normalizeInstructions(parsed));
  return getBoundingBoxFromInstructions(unarced);
};
var translateSegments = (segments, x, y) => {
  return segments.map((segment) => {
    if (segment.type === "a" || segment.type === "c" || segment.type === "v" || segment.type === "s" || segment.type === "h" || segment.type === "l" || segment.type === "m" || segment.type === "q" || segment.type === "t") {
      return segment;
    }
    if (segment.type === "V") {
      return {
        type: "V",
        y: segment.y + y
      };
    }
    if (segment.type === "H") {
      return {
        type: "H",
        x: segment.x + x
      };
    }
    if (segment.type === "A") {
      return {
        type: "A",
        rx: segment.rx,
        ry: segment.ry,
        largeArcFlag: segment.largeArcFlag,
        sweepFlag: segment.sweepFlag,
        xAxisRotation: segment.xAxisRotation,
        x: segment.x + x,
        y: segment.y + y
      };
    }
    if (segment.type === "Z") {
      return segment;
    }
    if (segment.type === "C") {
      return {
        type: "C",
        cp1x: segment.cp1x + x,
        cp1y: segment.cp1y + y,
        cp2x: segment.cp2x + x,
        cp2y: segment.cp2y + y,
        x: segment.x + x,
        y: segment.y + y
      };
    }
    if (segment.type === "Q") {
      return {
        type: "Q",
        cpx: segment.cpx + x,
        cpy: segment.cpy + y,
        x: segment.x + x,
        y: segment.y + y
      };
    }
    if (segment.type === "S") {
      return {
        type: "S",
        cpx: segment.cpx + x,
        cpy: segment.cpy + y,
        x: segment.x + x,
        y: segment.y + y
      };
    }
    if (segment.type === "T") {
      return {
        type: "T",
        x: segment.x + x,
        y: segment.y + y
      };
    }
    if (segment.type === "L") {
      return {
        type: "L",
        x: segment.x + x,
        y: segment.y + y
      };
    }
    if (segment.type === "M") {
      return {
        type: "M",
        x: segment.x + x,
        y: segment.y + y
      };
    }
    throw new Error(`Unknown segment type: ${segment.type}`);
  });
};
var translatePath = (path, x, y) => {
  return serializeInstructions(translateSegments(parsePath(path), x, y));
};
var resetPath = (d) => {
  const box = getBoundingBox(d);
  return translatePath(d, -box.x1, -box.y1);
};
var PathInternals = {
  getBoundingBoxFromInstructions,
  debugPath,
  cutPath
};

// ../../node_modules/.pnpm/@remotion+shapes@4.0.340_react-dom@19.2.5_react@19.2.5__react@19.2.5/node_modules/@remotion/shapes/dist/esm/index.mjs
var import_react = __toESM(require_react(), 1);
var import_react_dom = __toESM(require_react_dom(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
var getCoord = ({
  counterClockwise,
  actualProgress,
  rotation,
  radius,
  coord
}) => {
  const factor = counterClockwise ? -1 : 1;
  const val = Math[coord === "x" ? "cos" : "sin"](factor * actualProgress * Math.PI * 2 + Math.PI * 1.5 + rotation) * radius + radius;
  const rounded = Math.round(val * 1e5) / 1e5;
  return rounded;
};
var makePie = ({
  progress,
  radius,
  closePath = true,
  counterClockwise = false,
  rotation = 0
}) => {
  const actualProgress = Math.min(Math.max(progress, 0), 1);
  const endAngleX = getCoord({
    actualProgress,
    coord: "x",
    counterClockwise,
    radius,
    rotation
  });
  const endAngleY = getCoord({
    actualProgress,
    coord: "y",
    counterClockwise,
    radius,
    rotation
  });
  const start = {
    x: getCoord({
      actualProgress: 0,
      coord: "x",
      counterClockwise,
      radius,
      rotation
    }),
    y: getCoord({
      actualProgress: 0,
      coord: "y",
      counterClockwise,
      radius,
      rotation
    })
  };
  const end = { x: endAngleX, y: endAngleY };
  const instructions = [
    {
      type: "M",
      ...start
    },
    {
      type: "A",
      rx: radius,
      ry: radius,
      xAxisRotation: 0,
      largeArcFlag: false,
      sweepFlag: !counterClockwise,
      x: actualProgress <= 0.5 ? endAngleX : getCoord({
        actualProgress: 0.5,
        coord: "x",
        counterClockwise,
        radius,
        rotation
      }),
      y: actualProgress <= 0.5 ? endAngleY : getCoord({
        actualProgress: 0.5,
        coord: "y",
        counterClockwise,
        radius,
        rotation
      })
    },
    actualProgress > 0.5 ? {
      type: "A",
      rx: radius,
      ry: radius,
      xAxisRotation: 0,
      largeArcFlag: false,
      sweepFlag: !counterClockwise,
      ...end
    } : null,
    actualProgress > 0 && actualProgress < 1 && closePath ? {
      type: "L",
      x: radius,
      y: radius
    } : null,
    closePath ? {
      type: "Z"
    } : null
  ].filter(Boolean);
  const path = serializeInstructions(instructions);
  return {
    height: radius * 2,
    width: radius * 2,
    path,
    instructions,
    transformOrigin: `${radius} ${radius}`
  };
};
var shortenVector = (vector, radius) => {
  const [x, y] = vector;
  const currentLength = Math.sqrt(x * x + y * y);
  const scalingFactor = (currentLength - radius) / currentLength;
  return [x * scalingFactor, y * scalingFactor];
};
var scaleVectorToLength = (vector, length2) => {
  const [x, y] = vector;
  const currentLength = Math.sqrt(x * x + y * y);
  const scalingFactor = length2 / currentLength;
  return [x * scalingFactor, y * scalingFactor];
};
var joinPoints = (points, {
  edgeRoundness,
  cornerRadius,
  roundCornerStrategy
}) => {
  return points.map(([x, y], i2) => {
    const prevPointIndex = i2 === 0 ? points.length - 2 : i2 - 1;
    const prevPoint = points[prevPointIndex];
    const nextPointIndex = i2 === points.length - 1 ? 1 : i2 + 1;
    const nextPoint = points[nextPointIndex];
    const middleOfLine = [(x + nextPoint[0]) / 2, (y + nextPoint[1]) / 2];
    const prevPointMiddleOfLine = [
      (x + prevPoint[0]) / 2,
      (y + prevPoint[1]) / 2
    ];
    const prevVector = [x - prevPoint[0], y - prevPoint[1]];
    const nextVector = [nextPoint[0] - x, nextPoint[1] - y];
    if (i2 === 0) {
      if (edgeRoundness !== null) {
        return [
          {
            type: "M",
            x: middleOfLine[0],
            y: middleOfLine[1]
          }
        ];
      }
      if (cornerRadius !== 0) {
        const computeRadius = shortenVector(nextVector, cornerRadius);
        return [
          {
            type: "M",
            x: computeRadius[0] + x,
            y: computeRadius[1] + y
          }
        ];
      }
      return [
        {
          type: "M",
          x,
          y
        }
      ];
    }
    if (cornerRadius && edgeRoundness !== null) {
      throw new Error(`"cornerRadius" and "edgeRoundness" cannot be specified at the same time.`);
    }
    if (edgeRoundness === null) {
      if (cornerRadius === 0) {
        return [
          {
            type: "L",
            x,
            y
          }
        ];
      }
      const prevVectorMinusRadius = shortenVector(prevVector, cornerRadius);
      const prevVectorLength = scaleVectorToLength(prevVector, cornerRadius);
      const nextVectorMinusRadius = scaleVectorToLength(nextVector, cornerRadius);
      const firstDraw = [
        prevPoint[0] + prevVectorMinusRadius[0],
        prevPoint[1] + prevVectorMinusRadius[1]
      ];
      return [
        {
          type: "L",
          x: firstDraw[0],
          y: firstDraw[1]
        },
        roundCornerStrategy === "arc" ? {
          type: "a",
          rx: cornerRadius,
          ry: cornerRadius,
          xAxisRotation: 0,
          dx: prevVectorLength[0] + nextVectorMinusRadius[0],
          dy: prevVectorLength[1] + nextVectorMinusRadius[1],
          largeArcFlag: false,
          sweepFlag: true
        } : {
          type: "C",
          x: firstDraw[0] + prevVectorLength[0] + nextVectorMinusRadius[0],
          y: firstDraw[1] + prevVectorLength[1] + nextVectorMinusRadius[1],
          cp1x: x,
          cp1y: y,
          cp2x: x,
          cp2y: y
        }
      ];
    }
    const controlPoint1 = [
      prevPointMiddleOfLine[0] + prevVector[0] * edgeRoundness * 0.5,
      prevPointMiddleOfLine[1] + prevVector[1] * edgeRoundness * 0.5
    ];
    const controlPoint2 = [
      middleOfLine[0] - nextVector[0] * edgeRoundness * 0.5,
      middleOfLine[1] - nextVector[1] * edgeRoundness * 0.5
    ];
    return [
      {
        type: "C",
        cp1x: controlPoint1[0],
        cp1y: controlPoint1[1],
        cp2x: controlPoint2[0],
        cp2y: controlPoint2[1],
        x: middleOfLine[0],
        y: middleOfLine[1]
      }
    ];
  }).flat(1);
};
var star = ({
  centerX,
  centerY,
  points,
  innerRadius,
  outerRadius,
  cornerRadius,
  edgeRoundness
}) => {
  const degreeIncrement = Math.PI * 2 / (points * 2);
  const d = new Array(points * 2).fill(true).map((_p, i2) => {
    const radius = i2 % 2 === 0 ? outerRadius : innerRadius;
    const angle = degreeIncrement * i2 - Math.PI / 2;
    const point = {
      x: centerX + radius * Math.cos(angle),
      y: centerY + radius * Math.sin(angle)
    };
    return [point.x, point.y];
  });
  return [
    ...joinPoints([...d, d[0]], {
      edgeRoundness,
      cornerRadius,
      roundCornerStrategy: cornerRadius > 0 ? "bezier" : "arc"
    }),
    { type: "Z" }
  ];
};
var makeStar = ({
  points,
  innerRadius,
  outerRadius,
  cornerRadius = 0,
  edgeRoundness = null
}) => {
  const width = outerRadius * 2;
  const height = outerRadius * 2;
  const centerX = width / 2;
  const centerY = height / 2;
  const starPathInstructions = star({
    centerX,
    centerY,
    points,
    innerRadius,
    outerRadius,
    cornerRadius,
    edgeRoundness
  });
  const reduced = reduceInstructions(starPathInstructions);
  const path = resetPath(serializeInstructions(reduced));
  const boundingBox = PathInternals.getBoundingBoxFromInstructions(reduced);
  return {
    path,
    width: boundingBox.width,
    height: boundingBox.height,
    transformOrigin: `${centerX} ${centerY}`,
    instructions: starPathInstructions
  };
};

// ../../node_modules/.pnpm/@designcombo+transitions@5.5.8_react-dom@19.2.5_react@19.2.5__react@19.2.5_remotion@4.0_144fab71c561b49c3f85e1b533dd12b5/node_modules/@designcombo/transitions/dist/index.es.js
var A = (e) => ({
  getDurationInFrames: () => e.durationInFrames,
  getProgress: ({ frame: t }) => interpolate(t, [0, e.durationInFrames], [0, 1], {
    easing: e.easing,
    extrapolateLeft: "clamp",
    extrapolateRight: "clamp"
  })
});
var ct = (e = {}) => ({
  getDurationInFrames: ({ fps: t }) => e.durationInFrames ? e.durationInFrames : measureSpring({
    config: e.config,
    threshold: e.durationRestThreshold,
    fps: t
  }),
  getProgress: ({ fps: t, frame: r }) => {
    const o = e.reverse ? 0 : 1, a = e.reverse ? 1 : 0;
    return spring({
      fps: t,
      frame: r,
      to: o,
      from: a,
      config: e.config,
      durationInFrames: e.durationInFrames,
      durationRestThreshold: e.durationRestThreshold,
      reverse: e.reverse
    });
  }
});
var se = { exports: {} };
var G = {};
var fe;
function Fe() {
  return fe || (fe = 1, (function() {
    function e(n) {
      if (n == null) return null;
      if (typeof n == "function")
        return n.$$typeof === C ? null : n.displayName || n.name || null;
      if (typeof n == "string") return n;
      switch (n) {
        case R:
          return "Fragment";
        case S:
          return "Profiler";
        case N:
          return "StrictMode";
        case O:
          return "Suspense";
        case te:
          return "SuspenseList";
        case X:
          return "Activity";
      }
      if (typeof n == "object")
        switch (typeof n.tag == "number" && console.error(
          "Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."
        ), n.$$typeof) {
          case K:
            return "Portal";
          case le:
            return (n.displayName || "Context") + ".Provider";
          case I:
            return (n._context.displayName || "Context") + ".Consumer";
          case F:
            var c = n.render;
            return n = n.displayName, n || (n = c.displayName || c.name || "", n = n !== "" ? "ForwardRef(" + n + ")" : "ForwardRef"), n;
          case U:
            return c = n.displayName || null, c !== null ? c : e(n.type) || "Memo";
          case d:
            c = n._payload, n = n._init;
            try {
              return e(n(c));
            } catch {
            }
        }
      return null;
    }
    function t(n) {
      return "" + n;
    }
    function r(n) {
      try {
        t(n);
        var c = false;
      } catch {
        c = true;
      }
      if (c) {
        c = console;
        var m = c.error, v = typeof Symbol == "function" && Symbol.toStringTag && n[Symbol.toStringTag] || n.constructor.name || "Object";
        return m.call(
          c,
          "The provided key is an unsupported type %s. This value must be coerced to a string before using it here.",
          v
        ), t(n);
      }
    }
    function o(n) {
      if (n === R) return "<>";
      if (typeof n == "object" && n !== null && n.$$typeof === d)
        return "<...>";
      try {
        var c = e(n);
        return c ? "<" + c + ">" : "<...>";
      } catch {
        return "<...>";
      }
    }
    function a() {
      var n = z.A;
      return n === null ? null : n.getOwner();
    }
    function l() {
      return Error("react-stack-top-frame");
    }
    function f(n) {
      if (P.call(n, "key")) {
        var c = Object.getOwnPropertyDescriptor(n, "key").get;
        if (c && c.isReactWarning) return false;
      }
      return n.key !== void 0;
    }
    function u(n, c) {
      function m() {
        y || (y = true, console.error(
          "%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://react.dev/link/special-props)",
          c
        ));
      }
      m.isReactWarning = true, Object.defineProperty(n, "key", {
        get: m,
        configurable: true
      });
    }
    function s() {
      var n = e(this.type);
      return k[n] || (k[n] = true, console.error(
        "Accessing element.ref was removed in React 19. ref is now a regular prop. It will be removed from the JSX Element type in a future release."
      )), n = this.props.ref, n !== void 0 ? n : null;
    }
    function g(n, c, m, v, L, M, re, ne) {
      return m = M.ref, n = {
        $$typeof: T,
        type: n,
        key: c,
        props: M,
        _owner: L
      }, (m !== void 0 ? m : null) !== null ? Object.defineProperty(n, "ref", {
        enumerable: false,
        get: s
      }) : Object.defineProperty(n, "ref", { enumerable: false, value: null }), n._store = {}, Object.defineProperty(n._store, "validated", {
        configurable: false,
        enumerable: false,
        writable: true,
        value: 0
      }), Object.defineProperty(n, "_debugInfo", {
        configurable: false,
        enumerable: false,
        writable: true,
        value: null
      }), Object.defineProperty(n, "_debugStack", {
        configurable: false,
        enumerable: false,
        writable: true,
        value: re
      }), Object.defineProperty(n, "_debugTask", {
        configurable: false,
        enumerable: false,
        writable: true,
        value: ne
      }), Object.freeze && (Object.freeze(n.props), Object.freeze(n)), n;
    }
    function x(n, c, m, v, L, M, re, ne) {
      var b = c.children;
      if (b !== void 0)
        if (v)
          if (_(b)) {
            for (v = 0; v < b.length; v++)
              j(b[v]);
            Object.freeze && Object.freeze(b);
          } else
            console.error(
              "React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead."
            );
        else j(b);
      if (P.call(c, "key")) {
        b = e(n);
        var W = Object.keys(c).filter(function(ve) {
          return ve !== "key";
        });
        v = 0 < W.length ? "{key: someKey, " + W.join(": ..., ") + ": ...}" : "{key: someKey}", V[b + v] || (W = 0 < W.length ? "{" + W.join(": ..., ") + ": ...}" : "{}", console.error(
          `A props object containing a "key" prop is being spread into JSX:
  let props = %s;
  <%s {...props} />
React keys must be passed directly to JSX without using spread:
  let props = %s;
  <%s key={someKey} {...props} />`,
          v,
          b,
          W,
          b
        ), V[b + v] = true);
      }
      if (b = null, m !== void 0 && (r(m), b = "" + m), f(c) && (r(c.key), b = "" + c.key), "key" in c) {
        m = {};
        for (var oe in c)
          oe !== "key" && (m[oe] = c[oe]);
      } else m = c;
      return b && u(
        m,
        typeof n == "function" ? n.displayName || n.name || "Unknown" : n
      ), g(
        n,
        b,
        M,
        L,
        a(),
        m,
        re,
        ne
      );
    }
    function j(n) {
      typeof n == "object" && n !== null && n.$$typeof === T && n._store && (n._store.validated = 1);
    }
    var h = import_react2.default, T = Symbol.for("react.transitional.element"), K = Symbol.for("react.portal"), R = Symbol.for("react.fragment"), N = Symbol.for("react.strict_mode"), S = Symbol.for("react.profiler"), I = Symbol.for("react.consumer"), le = Symbol.for("react.context"), F = Symbol.for("react.forward_ref"), O = Symbol.for("react.suspense"), te = Symbol.for("react.suspense_list"), U = Symbol.for("react.memo"), d = Symbol.for("react.lazy"), X = Symbol.for("react.activity"), C = Symbol.for("react.client.reference"), z = h.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, P = Object.prototype.hasOwnProperty, _ = Array.isArray, E = console.createTask ? console.createTask : function() {
      return null;
    };
    h = {
      react_stack_bottom_frame: function(n) {
        return n();
      }
    };
    var y, k = {}, Y = h.react_stack_bottom_frame.bind(
      h,
      l
    )(), B = E(o(l)), V = {};
    G.Fragment = R, G.jsx = function(n, c, m, v, L) {
      var M = 1e4 > z.recentlyCreatedOwnerStacks++;
      return x(
        n,
        c,
        m,
        false,
        v,
        L,
        M ? Error("react-stack-top-frame") : Y,
        M ? E(o(n)) : B
      );
    }, G.jsxs = function(n, c, m, v, L) {
      var M = 1e4 > z.recentlyCreatedOwnerStacks++;
      return x(
        n,
        c,
        m,
        true,
        v,
        L,
        M ? Error("react-stack-top-frame") : Y,
        M ? E(o(n)) : B
      );
    };
  })()), G;
}
false ? se.exports = Pe() : se.exports = Fe();
var i = se.exports;
typeof window < "u" && (window.remotion_renderReady = false);
typeof window < "u" && (window.remotion_delayRenderTimeouts = {});
function _e(e, t) {
  const { allowFloats: r, component: o } = t;
  if (typeof e > "u")
    throw new Error(`The "durationInFrames" prop ${o} is missing.`);
  if (typeof e != "number")
    throw new Error(`The "durationInFrames" prop ${o} must be a number, but you passed a value of type ${typeof e}`);
  if (e <= 0)
    throw new TypeError(`The "durationInFrames" prop ${o} must be positive, but got ${e}.`);
  if (!r && e % 1 !== 0)
    throw new TypeError(`The "durationInFrames" prop ${o} must be an integer, but got ${e}.`);
  if (!Number.isFinite(e))
    throw new TypeError(`The "durationInFrames" prop ${o} must be finite, but got ${e}.`);
}
var ke = {
  validateDurationInFrames: _e
};
var xe = import_react2.default.createContext(null);
var Te = import_react2.default.createContext(null);
var me = ({ presentationProgress: e, children: t }) => {
  const r = (0, import_react2.useMemo)(() => ({
    enteringProgress: e
  }), [e]);
  return i.jsx(xe.Provider, { value: r, children: t });
};
var he = ({ presentationProgress: e, children: t }) => {
  const r = (0, import_react2.useMemo)(() => ({
    exitingProgress: e
  }), [e]);
  return i.jsx(Te.Provider, { value: r, children: t });
};
var we = (e) => import_react2.default.Children.toArray(e).reduce((r, o) => o.type === import_react2.default.Fragment ? r.concat(
  we(
    o.props.children
  )
) : (r.push(o), r), []);
var Le = 0.01;
var Me = ({
  children: e,
  presentationProgress: t,
  presentationDirection: r,
  passedProps: { direction: o = "from-left", enterStyle: a, exitStyle: l }
}) => {
  const f = (0, import_react2.useMemo)(() => {
    const s = t === 1 ? t * 100 : t * 100 - Le;
    if (r === "exiting")
      switch (o) {
        case "from-left":
          return {
            transform: `translateX(${s}%)`
          };
        case "from-right":
          return {
            transform: `translateX(${-t * 100}%)`
          };
        case "from-top":
          return {
            transform: `translateY(${s}%)`
          };
        case "from-bottom":
          return {
            transform: `translateY(${-t * 100}%)`
          };
        default:
          throw new Error(`Invalid direction: ${o}`);
      }
    switch (o) {
      case "from-left":
        return {
          transform: `translateX(${-100 + t * 100}%)`
        };
      case "from-right":
        return {
          transform: `translateX(${100 - s}%)`
        };
      case "from-top":
        return {
          transform: `translateY(${-100 + t * 100}%)`
        };
      case "from-bottom":
        return {
          transform: `translateY(${100 - s}%)`
        };
      default:
        throw new Error(`Invalid direction: ${o}`);
    }
  }, [r, t, o]), u = (0, import_react2.useMemo)(() => ({
    width: "100%",
    height: "100%",
    justifyContent: "center",
    alignItems: "center",
    ...f,
    ...r === "entering" ? a : l
  }), [f, a, l, r]);
  return i.jsx(AbsoluteFill, { style: u, children: e });
};
var ee = (e) => ({
  component: Me,
  props: {}
});
var Ae = ke.validateDurationInFrames;
var Q = function(e) {
  return null;
};
var ce = ({ children: e }) => i.jsx(i.Fragment, { children: e });
var Oe = ({
  children: e
}) => {
  const { fps: t } = useVideoConfig(), r = useCurrentFrame(), o = (0, import_react2.useMemo)(() => {
    let a = 0, l = 0;
    const f = we(e);
    return import_react2.Children.map(f, (u, s) => {
      const g = u;
      if (typeof g == "string") {
        if (g.trim() === "")
          return null;
        throw new TypeError(
          `The <TransitionSeries /> component only accepts a list of <TransitionSeries.Sequence /> components as its children, but you passed a string "${g}"`
        );
      }
      const x = f[s - 1], j = f[s + 1], h = typeof x == "string" || typeof x > "u" ? null : x.type === Q ? x : null, T = typeof j == "string" || typeof j > "u" ? null : j.type === Q ? j : null, K = typeof x == "string" || typeof x > "u" ? false : x.type === Q;
      if (g.type === Q) {
        if (K)
          throw new TypeError(
            `A <TransitionSeries.Transition /> component must not be followed by another <TransitionSeries.Transition /> component (nth children = ${s - 1} and ${s})`
          );
        return null;
      }
      if (g.type !== ce)
        throw new TypeError(
          `The <TransitionSeries /> component only accepts a list of <TransitionSeries.Sequence /> and <TransitionSeries.Transition /> components as its children, but got ${g} instead`
        );
      const R = g, N = `index = ${s}, duration = ${R.props.durationInFrames}`;
      if (!(R != null && R.props.children))
        throw new TypeError(
          `A <TransitionSeries.Sequence /> component (${N}) was detected to not have any children. Delete it to fix this error.`
        );
      const S = R.props.durationInFrames, {
        durationInFrames: I,
        children: le,
        ...F
      } = R.props;
      Ae(S, {
        component: "of a <TransitionSeries.Sequence /> component",
        allowFloats: true
      });
      const O = R.props.offset ?? 0;
      if (Number.isNaN(O))
        throw new TypeError(
          `The "offset" property of a <TransitionSeries.Sequence /> must not be NaN, but got NaN (${N}).`
        );
      if (!Number.isFinite(O))
        throw new TypeError(
          `The "offset" property of a <TransitionSeries.Sequence /> must be finite, but got ${O} (${N}).`
        );
      if (O % 1 !== 0)
        throw new TypeError(
          `The "offset" property of a <TransitionSeries.Sequence /> must be finite, but got ${O} (${N}).`
        );
      const te = l + O;
      let U = 0;
      h && (U = h.props.timing.getDurationInFrames({
        fps: t
      }), a -= U / 2);
      let d = te - U / 2;
      l += S + O, d < 0 && (d = 0);
      const X = T ? T.props.timing.getProgress({
        frame: r - d - I + T.props.timing.getDurationInFrames({ fps: t }) / 2 + a,
        fps: t
      }) : null, C = h ? h.props.timing.getProgress({
        frame: r - d,
        fps: t
      }) : null, z = h ? h.props.timing.getProgress({
        frame: r - (d + I),
        fps: t
      }) : null;
      if (T && S < T.props.timing.getDurationInFrames({ fps: t }) / 2)
        throw new Error(
          `The duration of a <TransitionSeries.Sequence /> must not be shorter than the duration of the next <TransitionSeries.Transition />. The transition is ${T.props.timing.getDurationInFrames(
            { fps: t }
          )} frames long, but the sequence is only ${S} frames long (${N})`
        );
      if (h && S < h.props.timing.getDurationInFrames({ fps: t }) / 2)
        throw new Error(
          `The duration of a <TransitionSeries.Sequence /> must not be shorter than the duration of the previous <TransitionSeries.Transition />. The transition is ${h.props.timing.getDurationInFrames(
            { fps: t }
          )} frames long, but the sequence is only ${S} frames long (${N})`
        );
      if (T && h && z !== null && C !== null) {
        const P = T.props.presentation ?? ee(), _ = h.props.presentation ?? ee(), E = h.props.timing.getDurationInFrames({
          fps: t
        }), y = T.props.timing.getDurationInFrames({
          fps: t
        }), k = P.component, Y = _.component, B = r < +Math.floor(d) + E / 2, V = r > Math.floor(d) + Math.floor(I) + E / 2, n = V ? Math.floor(d) + Math.floor(I) + Math.floor(y / 2) : Math.floor(d), c = B ? Math.floor(d) : Math.floor(d) + E / 2, m = T.props.timing.getProgress({
          frame: r - (c + I - y / 2),
          fps: t
        }), v = r > d, L = r < d + I + y / 2 + E / 2;
        return i.jsx(
          Freeze,
          {
            active: L && v && (B || V),
            frame: n,
            children: i.jsx(
              Sequence,
              {
                from: c,
                durationInFrames: S + y / 2,
                ...F,
                name: F.name || "<TS.Sequence>",
                children: i.jsx(
                  k,
                  {
                    passedProps: P.props ?? {},
                    presentationDirection: "exiting",
                    presentationProgress: m,
                    presentationDurationInFrames: T.props.timing.getDurationInFrames(
                      { fps: t }
                    ),
                    children: i.jsx(
                      he,
                      {
                        presentationProgress: m,
                        children: i.jsx(
                          Y,
                          {
                            passedProps: _.props ?? {},
                            presentationDirection: "entering",
                            presentationProgress: C,
                            presentationDurationInFrames: h.props.timing.getDurationInFrames(
                              { fps: t }
                            ),
                            children: i.jsx(
                              me,
                              {
                                presentationProgress: C,
                                children: u
                              }
                            )
                          }
                        )
                      }
                    )
                  }
                )
              },
              s
            )
          },
          s
        );
      }
      if (C !== null && h) {
        const P = h.props.timing.getDurationInFrames({ fps: t }) / 2, _ = h.props.presentation ?? ee(), E = _.component, y = r < d + P, k = y ? Math.floor(d) : Math.floor(d) + P, Y = r > k;
        return i.jsx(
          Freeze,
          {
            active: Y && y,
            frame: k,
            children: i.jsx(
              Sequence,
              {
                from: k,
                durationInFrames: S,
                ...F,
                name: F.name || "<TS.Sequence>",
                children: i.jsx(
                  E,
                  {
                    passedProps: _.props ?? {},
                    presentationDirection: "entering",
                    presentationProgress: C,
                    presentationDurationInFrames: h.props.timing.getDurationInFrames(
                      { fps: t }
                    ),
                    children: i.jsx(
                      me,
                      {
                        presentationProgress: C,
                        children: u
                      }
                    )
                  }
                )
              },
              s
            )
          },
          s
        );
      }
      if (X !== null && T) {
        const P = T.props.timing.getDurationInFrames({ fps: t }) / 2, _ = T.props.presentation ?? ee(), E = _.component, y = r > Math.floor(I) + Math.floor(d), k = r < Math.floor(I) + Math.floor(d) + P;
        return i.jsx(
          Freeze,
          {
            active: k && y,
            frame: Math.floor(I),
            children: i.jsx(
              Sequence,
              {
                from: Math.floor(d),
                durationInFrames: S + P,
                ...F,
                name: F.name || "<TS.Sequence>",
                children: i.jsx(
                  E,
                  {
                    passedProps: _.props ?? {},
                    presentationDirection: "exiting",
                    presentationProgress: X,
                    presentationDurationInFrames: T.props.timing.getDurationInFrames(
                      { fps: t }
                    ),
                    children: i.jsx(
                      he,
                      {
                        presentationProgress: X,
                        children: u
                      }
                    )
                  }
                )
              },
              s
            )
          },
          s
        );
      }
      return i.jsx(
        Sequence,
        {
          from: Math.floor(d),
          durationInFrames: S,
          ...F,
          name: F.name || "<TS.Sequence>",
          children: u
        },
        s
      );
    });
  }, [e, t, r]);
  return i.jsx(i.Fragment, { children: o });
};
var $ = ({ children: e, name: t, layout: r, ...o }) => {
  const a = t ?? "<TransitionSeries>", l = r ?? "absolute-fill";
  return i.jsx(Sequence, { name: a, layout: l, ...o, children: i.jsx(Oe, { children: e }) });
};
$.Sequence = ce;
$.Transition = Q;
Internals.addSequenceStackTraces($);
Internals.addSequenceStackTraces(ce);
var lt = () => {
  const e = import_react2.default.useContext(xe), t = import_react2.default.useContext(Te);
  return !e && !t ? {
    isInTransitionSeries: false,
    entering: 1,
    exiting: 0
  } : {
    isInTransitionSeries: true,
    entering: (e == null ? void 0 : e.enteringProgress) ?? 1,
    exiting: (t == null ? void 0 : t.exitingProgress) ?? 0
  };
};
var Ne = ({
  children: e,
  presentationDirection: t,
  presentationProgress: r,
  passedProps: o
}) => {
  const l = Math.sqrt(o.width ** 2 + o.height ** 2) / 2 * r, f = qe(
    o.width / 2,
    o.height / 2,
    l
  ), [u] = (0, import_react2.useState)(() => String(random(null))), s = (0, import_react2.useMemo)(() => ({
    width: "100%",
    height: "100%",
    clipPath: t === "exiting" ? void 0 : `url(#${u})`
  }), [u, t]);
  return i.jsxs(AbsoluteFill, { children: [
    i.jsx(AbsoluteFill, { style: s, children: e }),
    t === "exiting" ? null : i.jsx(AbsoluteFill, { children: i.jsx("svg", { width: o.width, height: o.height, children: i.jsx("defs", { children: i.jsx("clipPath", { id: u, children: i.jsx("path", { d: f, fill: "black" }) }) }) }) })
  ] });
};
var Ce = (e) => ({ component: Ne, props: e });
var qe = (e, t, r) => `M ${e}, ${t}
            m -${r}, 0
            a ${r},${r} 0 1,0 ${r * 2},0
            a ${r},${r} 0 1,0 -${r * 2},0`;
var Ye = ({
  children: e,
  presentationDirection: t,
  presentationProgress: r,
  passedProps: o
}) => {
  const { width: a, height: l } = o, f = a * r, u = l * r, s = a / 2 - f / 2, g = l / 2 - u / 2, [x] = (0, import_react2.useState)(() => String(random(null))), j = (0, import_react2.useMemo)(() => ({
    width: "100%",
    height: "100%",
    clipPath: t === "exiting" ? void 0 : `url(#${x})`
  }), [x, t]);
  return i.jsxs(AbsoluteFill, { children: [
    i.jsx(AbsoluteFill, { style: j, children: e }),
    t === "exiting" ? null : i.jsx(AbsoluteFill, { children: i.jsx("svg", { children: i.jsx("defs", { children: i.jsx("clipPath", { id: x, children: i.jsx(
      "rect",
      {
        x: s,
        y: g,
        width: f,
        height: u,
        fill: "black"
      }
    ) }) }) }) })
  ] });
};
var We = (e) => ({ component: Ye, props: e });
var De = ({
  children: e,
  presentationDirection: t,
  presentationProgress: r,
  passedProps: o
}) => {
  const a = Math.sqrt(o.width ** 2 + o.height ** 2) / 2, l = a * r, f = a * 2 * r, { path: u } = makeStar({
    innerRadius: l,
    outerRadius: f,
    points: 5
  }), s = getBoundingBox(u), g = translatePath(
    u,
    o.width / 2 - s.width / 2,
    o.height / 2 - s.height / 2
  ), [x] = (0, import_react2.useState)(() => String(random(null))), j = (0, import_react2.useMemo)(() => ({
    width: "100%",
    height: "100%",
    clipPath: t === "exiting" ? void 0 : `url(#${x})`
  }), [x, t]);
  return i.jsxs(AbsoluteFill, { children: [
    i.jsx(AbsoluteFill, { style: j, children: e }),
    t === "exiting" ? null : i.jsx(AbsoluteFill, { children: i.jsx("svg", { children: i.jsx("defs", { children: i.jsx("clipPath", { id: x, children: i.jsx("path", { d: g, fill: "black" }) }) }) }) })
  ] });
};
var Ze = (e) => ({ component: De, props: e });
var Ue = ({
  children: e,
  presentationDirection: t,
  presentationProgress: r,
  passedProps: o
}) => {
  const { width: a, height: l } = o, f = a * r, u = a / 2 - f / 2, [s] = (0, import_react2.useState)(() => String(random(null))), g = (0, import_react2.useMemo)(() => ({
    width: "100%",
    height: "100%",
    clipPath: t === "exiting" ? void 0 : `url(#${s})`
  }), [s, t]);
  return i.jsxs(AbsoluteFill, { children: [
    i.jsx(AbsoluteFill, { style: g, children: e }),
    t === "exiting" ? null : i.jsx(AbsoluteFill, { children: i.jsx("svg", { children: i.jsx("defs", { children: i.jsx("clipPath", { id: s, children: i.jsx(
      "rect",
      {
        x: u,
        y: 0,
        width: f,
        height: l,
        fill: "black"
      }
    ) }) }) }) })
  ] });
};
var Xe = (e) => ({ component: Ue, props: e });
var ze = ({ children: e, presentationDirection: t, presentationProgress: r, passedProps: o }) => {
  const a = t === "entering", l = (0, import_react2.useMemo)(() => ({
    opacity: a ? r : o.shouldFadeOutExitingScene ? 1 - r : 1,
    ...t === "entering" ? o.enterStyle : o.exitStyle
  }), [
    a,
    o.enterStyle,
    o.exitStyle,
    o.shouldFadeOutExitingScene,
    t,
    r
  ]);
  return i.jsx(AbsoluteFill, {
    style: l,
    children: e
  });
};
var de = (e) => ({
  component: ze,
  props: e ?? {}
});
var Be = 0.01;
var Ve = ({
  children: e,
  presentationProgress: t,
  presentationDirection: r,
  passedProps: { direction: o = "from-left", enterStyle: a, exitStyle: l }
}) => {
  const f = (0, import_react2.useMemo)(() => {
    const s = t === 1 ? t * 100 : t * 100 - Be;
    if (r === "exiting")
      switch (o) {
        case "from-left":
          return {
            transform: `translateX(${s}%)`
          };
        case "from-right":
          return {
            transform: `translateX(${-t * 100}%)`
          };
        case "from-top":
          return {
            transform: `translateY(${s}%)`
          };
        case "from-bottom":
          return {
            transform: `translateY(${-t * 100}%)`
          };
        default:
          throw new Error(`Invalid direction: ${o}`);
      }
    switch (o) {
      case "from-left":
        return {
          transform: `translateX(${-100 + t * 100}%)`
        };
      case "from-right":
        return {
          transform: `translateX(${100 - s}%)`
        };
      case "from-top":
        return {
          transform: `translateY(${-100 + t * 100}%)`
        };
      case "from-bottom":
        return {
          transform: `translateY(${100 - s}%)`
        };
      default:
        throw new Error(`Invalid direction: ${o}`);
    }
  }, [r, t, o]), u = (0, import_react2.useMemo)(() => ({
    width: "100%",
    height: "100%",
    justifyContent: "center",
    alignItems: "center",
    ...f,
    ...r === "entering" ? a : l
  }), [f, a, l, r]);
  return i.jsx(AbsoluteFill, {
    style: u,
    children: e
  });
};
var Je = (e) => ({
  component: Ve,
  props: e ?? {}
});
var Ge = (e, t) => {
  switch (t) {
    case "from-left":
      return `
M 0 0
L ${e} 0
L ${e} 1
L 0 1
Z`;
    case "from-top-left":
      return `
M 0 0
L ${e * 2} 0
L 0 ${e * 2}
Z`;
    case "from-top":
      return `
M 0 0
L 1 0
L 1 ${e}
L 0 ${e}
Z`;
    case "from-top-right":
      return `
M 1 0
L ${1 - e * 2} 0
L 1 ${e * 2}
Z`;
    case "from-right":
      return `
M 1 0
L 1 1
L ${1 - e} 1
L ${1 - e} 0
Z`;
    case "from-bottom-right":
      return `
M 1 1
L ${1 - e * 2} 1
L 1 ${1 - e * 2}
Z`;
    case "from-bottom":
      return `
M 0 1
L 1 1
L 1 ${1 - e}
L 0 ${1 - e}
Z`;
    case "from-bottom-left":
      return `
M 0 1
L 0 ${1 - e * 2}
L ${e * 2} 1
Z`;
    default:
      throw new Error(`Unknown direction ${JSON.stringify(t)}`);
  }
};
var He = (e, t) => {
  switch (t) {
    case "from-left":
      return `
M 1 1
L ${1 - e} 1
L ${1 - e} 0
L 1 0
Z`;
    case "from-top-left":
      return `
M 1 1
L ${1 - 2 * e} 1
L 1 ${1 - 2 * e}
Z`;
    case "from-top":
      return `
M 1 1
L 0 1
L 0 ${1 - e}
L 1 ${1 - e}
Z`;
    case "from-top-right":
      return `
M 0 1
L ${e * 2} 1
L 0 ${1 - e * 2}
Z`;
    case "from-right":
      return `
M 0 0
L ${e} 0
L ${e} 1
L 0 1
Z`;
    case "from-bottom-right":
      return `
M 0 0
L ${e * 2} 0
L 0 ${e * 2}
Z`;
    case "from-bottom":
      return `
M 1 0
L 0 0
L 0 ${e}
L 1 ${e}
Z`;
    case "from-bottom-left":
      return `
M 1 0
L ${1 - e * 2} 0
L 1 ${e * 2}
Z`;
    default:
      throw new Error(`Unknown direction ${JSON.stringify(t)}`);
  }
};
var Qe = ({
  children: e,
  presentationProgress: t,
  presentationDirection: r,
  passedProps: {
    direction: o = "from-left",
    innerEnterStyle: a,
    innerExitStyle: l,
    outerEnterStyle: f,
    outerExitStyle: u
  }
}) => {
  const [s] = (0, import_react2.useState)(() => String(random(null))), g = r === "entering" ? t : 1 - t, x = r === "entering" ? Ge(g, o) : He(g, o), j = (0, import_react2.useMemo)(() => ({
    width: "100%",
    height: "100%",
    justifyContent: "center",
    alignItems: "center",
    clipPath: `url(#${s})`,
    ...r === "entering" ? a : l
  }), [s, a, l, r]), h = (0, import_react2.useMemo)(() => r === "entering" ? f : u, [f, u, r]), T = (0, import_react2.useMemo)(() => ({
    width: "100%",
    height: "100%",
    pointerEvents: "none"
  }), []);
  return i.jsxs(AbsoluteFill, {
    style: h,
    children: [
      i.jsx(AbsoluteFill, {
        style: j,
        children: e
      }),
      i.jsx(AbsoluteFill, {
        children: i.jsx("svg", {
          viewBox: "0 0 1 1",
          style: T,
          children: i.jsx("defs", {
            children: i.jsx("clipPath", {
              id: s,
              clipPathUnits: "objectBoundingBox",
              children: i.jsx("path", {
                d: x,
                fill: "black"
              })
            })
          })
        })
      })
    ]
  });
};
var Ke = (e) => ({
  component: Qe,
  props: e ?? {}
});
var et = ({
  children: e,
  presentationDirection: t,
  presentationProgress: r,
  passedProps: {
    direction: o = "from-left",
    perspective: a = 1e3,
    innerEnterStyle: l,
    innerExitStyle: f,
    outerEnterStyle: u,
    outerExitStyle: s
  }
}) => {
  const g = (0, import_react2.useMemo)(() => {
    const T = t === "entering" ? interpolate(r, [0, 1], [o === "from-right" || o === "from-top" ? 180 : -180, 0]) : interpolate(r, [0, 1], [0, o === "from-right" || o === "from-top" ? -180 : 180]);
    return {
      width: "100%",
      height: "100%",
      transform: `${o === "from-top" || o === "from-bottom" ? "rotateX" : "rotateY"}(${T}deg)`,
      backfaceVisibility: "hidden",
      WebkitBackfaceVisibility: "hidden",
      ...t === "entering" ? l : f
    };
  }, [
    o,
    l,
    f,
    t,
    r
  ]), x = (0, import_react2.useMemo)(() => ({
    perspective: a,
    transformStyle: "preserve-3d",
    ...t === "entering" ? u : s
  }), [u, s, a, t]);
  return i.jsx(AbsoluteFill, {
    style: x,
    children: i.jsx(AbsoluteFill, {
      style: g,
      children: e
    })
  });
};
var tt = (e) => ({ component: et, props: e ?? {} });
var rt = ({ children: e, presentationDirection: t, presentationProgress: r, passedProps: o }) => {
  const a = Math.sqrt(o.width ** 2 + o.height ** 2) / 2, { path: l } = makePie({
    radius: a,
    progress: r
  }), f = translatePath(l, -(a * 2 - o.width) / 2, -(a * 2 - o.height) / 2), [u] = (0, import_react2.useState)(() => String(random(null))), s = (0, import_react2.useMemo)(() => ({
    width: "100%",
    height: "100%",
    clipPath: t === "exiting" ? void 0 : `url(#${u})`,
    ...t === "entering" ? o.innerEnterStyle : o.innerExitStyle
  }), [
    u,
    o.innerEnterStyle,
    o.innerExitStyle,
    t
  ]), g = (0, import_react2.useMemo)(() => t === "entering" ? o.outerEnterStyle : o.outerExitStyle, [
    o.outerEnterStyle,
    o.outerExitStyle,
    t
  ]);
  return i.jsxs(AbsoluteFill, {
    style: g,
    children: [
      i.jsx(AbsoluteFill, {
        style: s,
        children: e
      }),
      t === "exiting" ? null : i.jsx(AbsoluteFill, {
        children: i.jsx("svg", {
          children: i.jsx("defs", {
            children: i.jsx("clipPath", {
              id: u,
              children: i.jsx("path", {
                d: f,
                fill: "black"
              })
            })
          })
        })
      })
    ]
  });
};
var nt = (e) => ({ component: rt, props: e ?? {} });
var ut = {
  none: ({ id: e }) => i.jsx(
    $.Transition,
    {
      presentation: de(),
      timing: A({ durationInFrames: 1 })
    },
    e
  ),
  fade: ({ durationInFrames: e, id: t }) => i.jsx(
    $.Transition,
    {
      presentation: de(),
      timing: A({ durationInFrames: e })
    },
    t
  ),
  slide: ({ durationInFrames: e, id: t, direction: r }) => i.jsx(
    $.Transition,
    {
      presentation: Je({ direction: r }),
      timing: A({ durationInFrames: e })
    },
    t
  ),
  wipe: ({ durationInFrames: e, id: t, direction: r }) => i.jsx(
    $.Transition,
    {
      presentation: Ke({ direction: r }),
      timing: A({ durationInFrames: e })
    },
    t
  ),
  flip: ({ durationInFrames: e, id: t }) => i.jsx(
    $.Transition,
    {
      presentation: tt(),
      timing: A({ durationInFrames: e })
    },
    t
  ),
  clockWipe: ({ width: e, height: t, durationInFrames: r, id: o }) => i.jsx(
    $.Transition,
    {
      presentation: nt({ width: e, height: t }),
      timing: A({ durationInFrames: r })
    },
    o
  ),
  star: ({ width: e, height: t, durationInFrames: r, id: o }) => i.jsx(
    $.Transition,
    {
      presentation: Ze({ width: e, height: t }),
      timing: A({ durationInFrames: r })
    },
    o
  ),
  circle: ({ width: e, height: t, durationInFrames: r, id: o }) => i.jsx(
    $.Transition,
    {
      presentation: Ce({ width: e, height: t }),
      timing: A({ durationInFrames: r })
    },
    o
  ),
  rectangle: ({ width: e, height: t, durationInFrames: r, id: o }) => i.jsx(
    $.Transition,
    {
      presentation: We({ width: e, height: t }),
      timing: A({ durationInFrames: r })
    },
    o
  ),
  slidingDoors: ({
    width: e,
    height: t,
    durationInFrames: r,
    id: o
  }) => i.jsx(
    $.Transition,
    {
      presentation: Xe({ width: e, height: t }),
      timing: A({ durationInFrames: r })
    },
    o
  )
};
export {
  $ as TransitionSeries,
  ut as Transitions,
  Ce as circle,
  nt as clockWipe,
  de as fade,
  tt as flip,
  A as linearTiming,
  We as rectangle,
  Je as slide,
  Xe as slidingDoors,
  ct as springTiming,
  Ze as star,
  lt as useTransitionProgress,
  Ke as wipe
};
/*! Bundled license information:

@designcombo/transitions/dist/index.es.js:
  (**
   * @license React
   * react-jsx-runtime.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

@designcombo/transitions/dist/index.es.js:
  (**
   * @license React
   * react-jsx-runtime.development.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)
*/
//# sourceMappingURL=@designcombo_transitions.js.map
